# TalkMate Portal — System Map

**Last updated:** 2026-05-31
**Last session:** 58 (Sprint 1 + follow-ups)
**Main SHA:** ffdffb9
**Next migration number:** 063
**Active spec:** Mobile Sales Rep Phase 2 sub-project 1 (Live API) — Stage 1 portal backend ✅ shipped to prod (session 54). Stage 2 mobile ✅ shipped on `talkmate-mobile` PR #1 (open, draft). Stage 3 (EAS Build + TestFlight + Play internal) blocked on Apple Developer + Google Play account sign-ups. Stage 4 (end-to-end audit on real TestFlight build) blocked on Stage 3. Spec: [`docs/superpowers/specs/2026-05-28-mobile-sales-rep-phase2-live-api-design.md`](docs/superpowers/specs/2026-05-28-mobile-sales-rep-phase2-live-api-design.md). Plan: [`docs/superpowers/plans/2026-05-28-mobile-sales-rep-phase2-live-api.md`](docs/superpowers/plans/2026-05-28-mobile-sales-rep-phase2-live-api.md).
**Repo:** irfanhanif89-art/talkmate-portal
**Production URL:** https://app.talkmate.com.au
**Supabase project:** mdsfdaefsxwrakgkyflr
**Vercel project:** prj_loxPaAwjRW2VV4qxQP7qpu7iq68k

---

## Session Log

| Session | Date | Branch | SHA | Migration(s) | Summary |
|---------|------|--------|-----|--------------|---------|
| 1–7 | 2025–2026 | — | — | 001–010 | Initial build, CRM, admin client management |
| 8 | 2026 | — | — | — | Self-serve signup flow |
| 9 | 2026 | — | — | 023 | Receptionist features |
| 10 | 2026 | — | — | 024 | Dispatcher system |
| Hotfix | 2026 | — | — | 025 | Duplicate-owner DB guard |
| 11 | 2026 | — | — | 026 | Security foundations + RBAC + audit log |
| 12 | 2026 | — | — | 027–029 | Services fix + TalkMate Command + Vapi call ID |
| 13 | 2026 | — | — | — | Admin portal parity + Sync Agent expansion |
| 14 | 2026 | — | — | 030 | Distance quoting engine + scheduler foundation |
| 15 | 2026 | — | — | 031 | Accounts, VIP bypass, native scheduler, Twilio SMS, waitlist, public holidays |
| 16 | 2026 | — | — | — | Locked preview pattern + scheduler route display |
| 17B | 2026 | — | — | — | Audit fixes — create_booking sync, Make.com retirement, check_caller logging |
| 18 | 2026 | — | — | 032 | Call Intelligence — AI-scored call quality, alerts, SMS recovery |
| 19 | 2026 | — | — | 033 | SMS visibility + AI SMS verification |
| 20 | 2026 | — | — | 034 | Admin Go-Live Verification Checklist |
| Hotfix 035 | 2026 | — | — | 035 | sms_used_this_month counter not incrementing |
| 21 | 2026 | — | — | 036 | Sales HQ — rep portal, CRM pipeline, commissions, contract signing, admin sales team |
| 22 | 2026-05-20 | feature/session-22-pricing | — | 037 | Pricing overhaul — setup fees, annual billing, 2.5% commission bonus |
| 22B | 2026-05-20 | — | — | — | Admin quality alerts — Telegram pings, daily digest |
| 23 | 2026-05-20 | feature/session-23-contractor-flow | — | 038 | Contractor agreement flow — invite, sign, PDF, commissions, termination |
| 24 | 2026 | feature/session-24-agent-stability | — | 039 | Agent health monitoring |
| 25 | 2026 | feature/session-25-unified-rep-lifecycle | — | 040 | Unified rep lifecycle — contractor → sales_rep provisioning, portal access |
| 26 | 2026-05-21 | feature/fix-contractor-signature-panel | 9674bd1 | 041 | Contractor signature panel — ABN mandatory, hard-fail upload, clawback, resend invite, Telegram alerts |
| 27 | 2026-05-22 | feature/session-27-revenue-fixes | e612c9b | 042 | Revenue fixes — Stripe real payment, /wl-preview public, SMS type constraint, clawback enforcement, sales rep add lead, hardcoded secrets removed |
| 28 | 2026-05-22 | feature/session-28-vapi-lifecycle | da63120 | 043 | Vapi lifecycle + call intelligence resilience — mandatory VAPI_WEBHOOK_SECRET, legacy business_id trust fixed, error-call retry widened to 7d, agent config standard restructured (required/requiredForBookings/requiredForQuoting), shared vapi-tool-defs module, plan-aware validator, onboarding builds validator-clean agents, approve-agent gated on go-live checklist |
| 29 | 2026-05-22 | feature/session-29-sms-confirmation-loop | 7508b4b | 044 | Hayden SMS confirmation loop — caller "received" SMS + dispatcher YES/NO loop on +61 480 847 945, /api/twilio/sms-reply with manual HMAC-SHA1, 15-min dispatcher reminder, new bookings columns (confirmation_ref/dispatcher_notified_at/reminder_sent_at/confirmed_by_phone), declined status, 5 new SMS types |
| 30 | 2026-05-22 | feature/session-30-fixes | d987bdc | 045 | Session 30 fixes — sync routes write `/api/vapi/functions` (not `/api/webhooks/vapi`), one-shot `/api/cron/backfill-server-url` cron to repair existing assistants, calls page loading fix, comma-separated ADMIN_EMAIL allowlist, dollar-sign validator exception for plan prices ($299/$499/$799 + 10× annual variants), admin PATCH whitelist expanded (7 account_status values + billing_cycle/setup_fee_waived/setup_fee_amount) + edit modal billing section, SMS failure Telegram alerts (twilio_error/config_missing/invalid_phone only), owner booking notification SMS type + template + createBooking call site, welcome email moved from onboarding/complete → admin/approve-agent (non-override path only), impersonate route gains `?redirect=1` + `?next=` modes, 7 admin stub pages collapsed into impersonation redirects |
| 31 | 2026-05-22 | feature/session-31-bookings-cleanup | 2ea89fb | — | Bookings cleanup — backfill cron retired (`/api/cron/backfill-server-url` + vercel.json entry deleted; both live Vapi agents verified by Donna), legacy Stripe routes deleted (`/api/stripe/checkout`, `/api/stripe/create-checkout-session` — zero callers), bookings UI rewritten to modern schema (scheduled_start/truck_type/description/pickup_address/dropoff_address/confirmation_ref/sms_confirmation_sent), route line + REF badge added, ConfirmModal date fixed, New Booking modal posts to existing `/api/portal/bookings` |
| 32 | 2026-05-22 | feature/session-32-dashboard-fixes | b1a08f1 | — | Dashboard fixes bundle — M34 nurture column added to `LEAD_STATUS_COLUMNS`, M5 dashboard label corrected to "Calls missed this month", M8 missed-call filter rewritten (dashboard query now selects `intelligence_status`; counts only `outcome === 'Missed'` OR null-outcome calls with terminal scored status `IN ('resolved','review','critical')` AND duration < 5s — pending/error are excluded), L5 annual ROI uses `biz.billing_cycle` for years × annual-price ($2990/$4990/$7990), M35 `commissionPaidEmailHtml` template + send on `/api/admin/commissions/[id]` pay action (rep + business via existing JOIN — no separate `businesses` query), M6 mailto Help link added to sidebar (rendered as `<a>` not `<Link>`), L1 hardcoded `+$6.20` and `$85` estimates centralised into `src/lib/dashboard-defaults.ts` |
| 33 | 2026-05-23 | feature/session-33-bookings-cleanup | a7dc649 | 046 | Bookings cleanup atomic unit — 5 files updated to drop legacy column references (`command-executor.ts` viewBookings now selects `truck_type/description/scheduled_start`; `admin-feature-tabs.tsx` `AdminBooking` interface modernised + display blocks use `truck_type ?? description` and `scheduled_start` formatted; admin and portal PATCH `ALLOWED_FIELDS` whitelists strip the 5 legacy fields while keeping `actual_start/actual_end/no_show/cancellation_reason`; `bookings-view.tsx` `Booking` interface drops 6 legacy optional fields, `formatScheduled()` simplified to `(booking: Booking)` with single `scheduled_start` branch, fallback chains and `ConfirmModal` cleaned up), Migration 046 drops 6 legacy bookings columns (`confirmation_sms_sent/booking_type/service_requested/preferred_date/preferred_time/notes`) with `DROP COLUMN IF EXISTS`, idempotent backfills + 4 pre-migration check SQL comments for Donna, Stripe pagination fix in `/api/cron/stripe-sync` (do/while loop on `starting_after` with 50-page safety cap = 5000 subs max) |
| 34 | 2026-05-23 | feature/session-34-proxima-demo | eecaa4d | — | Proxima white-label partner demo — new public route `/wl-preview/proxima/demo` shows Monique a Proxima-branded partner portal preview. Static hardcoded data only (4 sample agents, 5 sample calls, computed aggregates) in `src/lib/wl-demo-data.ts`; zero DB reads of business/call tables. Subdomain gate via `notFound()` ensures other partners can't accidentally serve Proxima's network. Parent login page (`src/app/wl-preview/[subdomain]/page.tsx`) gets a "View partner demo →" link gated on `subdomain === 'proxima'`. Brand tokens locked to navy `#1B4FBB`, secondary `#0A1E38`, accent `#E8622A`. Middleware bypass (`pathname.startsWith('/wl-preview')`) verified — already present from Session 27. No migration. |
| 35 | 2026-05-25 | feature/hotfix-alert-dedup | b5fe368 | 047 | Hotfix — agent health alert auto-resolve. Diagnosis: existing dedup window (2h on `issue_code`, gated INSERT before SELECT, Telegram fires inside the gate) was working as designed — the spam was caused by nothing ever auto-resolving alerts, so rows aged out of the 2h window and the cron re-inserted identical rows. Diagnostic SQL across 24h showed 9 (business_id, issue_code) pairs duplicated with avg gap ~138 min — confirming window-expiry pattern. Fix: surgical addition to `agent-health-check/route.ts` — after `validateAgentConfig` returns, before the per-issue alert loop, select all open `config_issue` alerts for the business and resolve any whose `issue_code` is no longer in the current issue set (`resolved_by = 'auto:config_issue_no_longer_detected'`). Gated on `alert_type = 'config_issue'` so webhook_gap and transcript_violation lifecycles are untouched. Safe vs Vapi GET failures (outer `continue` skips this block). Migration 047 marks accumulated duplicates as resolved (UPDATE-not-DELETE; keeps most recent open row per pair; 1-hour buffer to avoid racing live cron; `resolved_by = 'session_35_dedup_cleanup'`; idempotent). Rule 1 (insert dedup) and Rule 2 (Telegram inside gate) deliberately untouched — verified already correct in code. Demo agent `fdeef08c-341c-49b5-851a-524c4ab45fee` missing `check_availability` tool is Donna's post-merge task (DEPLOYMENT.md Step 5a). |
| 36 | 2026-05-25 | feature/hotfix-grok-scoring-migration | 2f407df | — | Hotfix — Call Intelligence scoring migrated from Anthropic Sonnet to Grok (`grok-4.20-0309-non-reasoning`) behind a `SCORING_PROVIDER` env var (default `anthropic`, set to `xai` to flip). `src/lib/call-intelligence.ts` refactored into a dispatcher: existing Anthropic logic moved into `scoreViaAnthropic(input)`, new `scoreViaGrok(input)` uses the existing `grokJson` helper. Identical system prompt + user prompt across providers; shared `coerceResult` validator (already strips unknown flag types, clamps score 1–10, normalises status, defaults `sms_verification`). Both new functions exported so `scripts/backtest-grok-scoring.ts` can compare 100 most-recent Claude-scored calls against Grok (READ-ONLY; writes verdict + per-call detail to `scripts/backtest-results.json`). Decision gate: ≤0.5 avg \|delta\|, ≥85% classification agreement at 5-boundary, ≥90% critical-flag recall, \|mean delta\| ≤ 0.3, ≤5% errors → cutover via `SCORING_PROVIDER=xai` in Vercel prod. Any bar fails → fall back to Haiku in `INTELLIGENCE_MODEL_ANTHROPIC`. Anthropic API key, env var, and code path preserved as fallback; rollback is one env-var flip + redeploy. Side fix: `src/lib/grok.ts:34` default model `grok-3` → `grok-4.20-0309-non-reasoning` (no existing callers). Admin status pill hint updated to reflect call-scoring role. No migration. Back-test run is operator-deferred — `.env.local` ships placeholders; needs real Supabase + Grok credentials. |
| 37 | 2026-05-25 | feature/hotfix-haiku-scoring | 2f407df | — | Hotfix — Add Claude Haiku scoring path. `SCORING_PROVIDER=haiku` now routes to a new `scoreViaHaiku(input)` that hits the Anthropic Messages API with model `claude-haiku-4-5-20251001` — same system prompt, same user-prompt builder, same `coerceResult` validator as `scoreViaAnthropic`, only the model string differs. `INTELLIGENCE_MODEL_HAIKU` constant added; `INTELLIGENCE_MODEL` re-export now resolves Haiku correctly at module load. Dispatcher widened: `'xai'` still routes to Grok, `'haiku'` to Haiku, anything else (including unset) stays on Sonnet — safe no-op deploy until Donna sets the env var. Motivation: Haiku is ~12× cheaper than Sonnet (~$0.25 vs ~$3.00 per M input tokens) for the same structured JSON task; at 30–40 calls/day this drops Anthropic spend from ~$0.60/day to ~$0.05/day and prevents the prepaid balance hitting zero. No back-test required — quality is empirically known for structured rubric scoring. Branched from `feature/hotfix-grok-scoring-migration` (not `dev`) because the dispatcher being modified only lives there until the Grok PR merges. No migration. |
| 38a | 2026-05-25 | feature/contractor-portal-fix-and-lead-import | b320a66 | — | Contractor portal fix + admin lead import. **Fix**: post-sign Resend portal-access email (`sendRepPortalAccessEmail`) was gated to `linkedExistingUser === true`, so new contractors got nothing when Supabase's invite email was delayed/spammed/rate-limited — exposed by Jade's dummy account (signed 21 May, never logged in). `src/app/api/contractor-onboarding/[token]/sign/route.ts` now always sends the Resend email after signing; failures fire a Telegram admin alert via `notifyAdminAlert` so silent drops cannot strand a signed contractor. `src/lib/sales-notify.ts` `sendRepPortalAccessEmail` returns its `sendEmail` result so callers can react. **Admin recovery**: new `POST /api/contractors/[id]/resend-portal-access` (admin-gated) refreshes Supabase invite + re-sends Resend email; new "Resend Portal Access" button on `/admin/contractors/[id]` (visible only when status='active' + agreement_signed_at). **Lead import (new)**: `/admin/leads-import` server-component page loads active reps; client component handles CSV upload, auto-detect column mapping (Business Name → business_name, Owner/Decision Maker → contact_name, Phone → phone, etc.), live preview, rep + industry defaults, bulk submit. `POST /api/admin/leads/bulk-import` validates rep is active, chunks inserts in batches of 500, 5000-row cap, returns inserted/skipped counts. New "Import Leads" sidebar item. Verified end-to-end on prod: Resend button on Jade returned 200 (email fired to jadebarber2812@gmail.com), `/admin/leads-import` renders clean. Validated against the towing leads sheet (53 rows) used to onboard Navya. No migration. |
| 38b | 2026-05-25 | feature/haiku-watcher-cron | 580de14 | — | 24/7 Haiku cutover watcher via Vercel cron. Replaces the Claude-Code-side scheduled task with a server-side cron so the watcher runs whether or not Claude Code is open. `src/app/api/cron/haiku-watcher/route.ts` runs hourly at :37 past with three branches: (1) Haiku scoring detected + no prior alert → Telegram with first call_id/scored_at/token counts + insert `system_alerts` row of type `haiku_cutover_confirmed` for dedup; (2) Haiku scoring detected + prior alert exists → no-op returns `{ status: 'already_fired' }`; (3) no Haiku scoring yet → regression check counting successful Sonnet entries since cutover, fires `haiku_cutover_regression` warning Telegram if non-zero. `vercel.json` adds `{ path: /api/cron/haiku-watcher, schedule: 37 * * * * }` (off-minute to avoid dogpiling the 24 existing crons). No migration. |
| 39 | 2026-05-25 | feature/session-39-audit-cleanup | 346b367 | — | Audit cleanup — 9 medium-severity items deferred from Session 38, executed in parallel by 3 subagents (Claude CoWork) with zero file overlap and consolidated. **Mobile admin sidebar** (`src/components/admin/AdminSidebarLayout.tsx`): below 768px the sidebar is hidden by default and slides in from the left as a 240px overlay over a dark backdrop when a fixed top-left hamburger is tapped; tapping a nav link, the in-drawer X, or the backdrop closes it; main content fills 100% viewport on mobile. Desktop ≥768px unchanged (240/64px expand/collapse, localStorage preference preserved). Hydration-safe via the existing `mounted` gate; `matchMedia` with Safari fallback + cleanup. **Sales API correctness** (4 files): `src/app/sales/layout.tsx` adds `onboarded_via, contractor_id` to the sales_reps select so the SalesRepRow cast is no longer type-unsound; `src/app/api/sales/onboard/route.ts` replaces `listUsers()` with `findAuthUserByEmail()` (auth.users > 50 was silently missing dups), properly awaits the `welcome_email_sent` DB flip (was dangling `.then()` in a serverless handler), and maps rep-collected industry → `business_type` enum via a new `mapIndustryToBusinessType()` helper against `BUSINESS_TYPE_CONFIG` (was hardcoded `'other'`, degrading Vapi prompts); `src/app/api/sales/leads/[id]/route.ts` returns explicit 400 with `Status changes must use the dedicated endpoints: /won, /lost, /bad-lead` when PATCH body.status is terminal (was silently dropping while updating other fields); `src/components/sales/contract-view.tsx` detects mobile via `matchMedia('(max-width: 767px)')` and replaces the blank-on-iOS iframe with a prominent full-width orange "Open contract PDF" button (desktop keeps iframe + new-tab link). **Email/notify hardening** (2 files): `src/lib/sales-notify.ts` renames `NEXT_PUBLIC_PORTAL_URL` → `NEXT_PUBLIC_APP_URL` (was the only file using PORTAL_URL; silent fallback to hardcoded prod URL on preview deploys) — grep across `src/` confirms zero remaining matches. `notifyWin`, `sendRepPortalAccessEmail`, `sendTerminationEmail` wrap their sendEmail calls so failures log + fire a Telegram alert via direct `sendTelegram` (avoids `notifyAdminAlert` recursion). `src/app/api/admin/sales-reps/invite/route.ts` replaces the bare `/login` "magic link" in the Resend backup invite email with a real `admin.auth.admin.generateLink()` action_link (type=invite for new users, type=magiclink for existing), with fallback to `/login?next=/sales/dashboard` + Telegram alert on generation failure; `.catch(() => {})` on the backup email send replaced with log + Telegram alert on both `{ok:false}` and throw. Verified end-to-end on prod: mobile 375px contractors page now shows hamburger + readable table with full contractor names (Navya Baiyer, Jade Barber, full emails); desktop 1280px sidebar unchanged at 240px static; zero console errors; tsc clean. No migration. |
| 40 | 2026-05-25 | feature/session-40-polish | ebf4178 | — | Session 40 polish bundle — final cleanup of nits from the Session 39 deferral list. 13 files, +35/-17. **Hydration fix** (`src/lib/sales-format.ts`): `formatDate` and `formatDateTime` now pass `timeZone: 'Australia/Brisbane'` to `Intl.DateTimeFormat`. Server (Node UTC) and client (browser local) previously produced different strings and React fired hydration warning #418 on every date-rendering page including `/admin/contractors/[id]`. TalkMate operates from QLD so Brisbane is the canonical reading. Verified on prod against Jade's record: zero console errors. **Mobile rate-card grid** (`src/components/sales/commission-policy-modal.tsx`): `gridTemplateColumns: '1fr 1fr 1fr'` → `repeat(auto-fit, minmax(90px, 1fr))` so the rate cards wrap gracefully on 375px instead of overflowing. **Sign-out full reload** (`src/components/sales/sales-nav.tsx`): `router.push('/login')` → `window.location.href = '/login?next=/sales/dashboard'` so supabase client cookies/state are flushed before the next render (avoids brief stale-session flashes) and the rep is sent back to /sales/dashboard after re-auth. **Em-dash copy cleanup** in user-facing strings across 5 files: `sales-notify.ts` (8 email subjects + body headlines + Telegram message lines now use colons/periods instead of em-dashes for clearer parsing in inbox previews), `commission-policy-modal.tsx` ("I Agree, Let's Go"), `contract-view.tsx` (error fallback + "IP and timestamp" softened to "We keep a date-stamped record so you have proof of what you signed"), `onboard-form.tsx` (deal-option separator → middle dot). The literal '—' used as null-data placeholder (formatDate's empty branch, table empty cells) is typography and deliberately kept. **Per-page browser titles**: each of the 7 `/sales/*/page.tsx` server pages now exports `metadata = { title: 'X — TalkMate Sales HQ' }` (Dashboard / My Pipeline / My Clients / Onboard Client / Commissions / My Contract / Profile) so browser tabs show useful titles instead of the generic root layout title. No migration, no new env vars, no new dependencies. tsc clean. |

| 41 | 2026-05-26 | feature/session-41-sales-hq-tools | 86d683c | 049 | Sales HQ Tools — demo launcher (`/sales/demo`, 14 industry cards, Vapi agent swap with `ALLOWED_CURRENT_ASSISTANTS` guard), sales profile reply-to email (`/sales/profile` + `MissingEmailBanner`), onboarding queue (`/admin/onboarding-queue` + wizard at `/admin/onboarding-queue/[id]`), proposal generator (`/sales/leads/[id]/proposal`) with open tracking via Svix-verified Resend webhook (`/api/webhooks/resend`, `email.opened`), follow-up sequencer cron (`/api/cron/process-followups` at `:50` past the hour), hit list (`/sales/hitlist`), NotificationBell with realtime `rep_notifications`, WonConfirmationScreen with welcome-call script, "Closed by rep" column on admin clients view. Migration 049 adds `lead_followups`, `rep_notifications`, `proposal_tracking`, `increment_proposal_opens(text)` RPC (service_role-only, pinned search_path), `sales_reps.notification_email`, 6 `businesses` columns, `client_comms_log` lead_id support. Provisioning core extracted into `src/lib/provisioning/approveAgent.ts` with application-level Twilio idempotency. svix@^1.94 added. 4 new env vars: `VAPI_DEMO_PHONE_NUMBER_ID`, `NEXT_PUBLIC_DEMO_PHONE_DISPLAY`, `SALES_EMAIL_FROM`, `RESEND_WEBHOOK_SECRET`. |
| 42 | 2026-05-26 | feature/session-42-lifecycle-integrity | 2a5dbc0 | 050 + 051 | Lifecycle integrity bundle — closes H8, H10, plus Stripe webhook idempotency. Council pressure-tested before any code was written; produced 2 decision docs (H8: Lever 2 vs Lever 1; H10: planFromStripePriceNickname source-of-truth). **H8 (Vapi entitlement)**: new `src/lib/vapi-phone.ts` (`unassignVapiPhone` + `reassignVapiPhone`) PATCHes `https://api.vapi.ai/phone-number/{id}` setting `assistantId = null` on cancel/expire/suspend, restores on reactivation. The assistant body is never touched: model, voice, transcriber, tools, server URL, system prompt, knowledge base all preserved. Wired into `customer.subscription.deleted` webhook + `expire-trials` cron + admin `cancel/suspend/end-trial` routes; `reassignVapiPhone` wired into `customer.subscription.updated` (status=active branch) + admin `activate` route. `src/lib/provisioning/approveAgent.ts` now captures the Vapi POST response and persists `vapi_phone_number_id` on businesses going forward (was discarded before). `src/app/api/onboarding/complete/route.ts` persists `PREVIEW_VAPI_PHONE_ID` so preview-stage suspensions can also fire. `/api/vapi/functions` Vapi-native branch now gates on `account_status IN ('active','trial')` — defence-in-depth against any unbinding that fails (the legacy branch already had this gate since Session 28). **H10 (plan source-of-truth)**: replaced 3 occurrences of `(nickname || 'starter').toLowerCase()` in `/api/webhooks/stripe` with `planFromStripePriceNickname()`; `customer.subscription.updated` rewritten to compare-and-update `businesses.plan` (silent renewal storms stop spamming Telegram — Stripe fires this event on every billing cycle, not just plan changes) + insert `notifications` row + re-bind Vapi phone if previously unassigned + flip account_status back to active. Pre-existing bug in `sendWelcomeEmail` plan label fixed (was treating `'pro'` as Starter because it only branched on `'professional'`). **Stripe webhook idempotency**: new `public.stripe_webhook_events (event_id PK, event_type, received_at, processed_at)` dedup table (admin RLS); top-of-handler check no-ops on duplicate `event.id` then INSERTs; bottom-of-handler stamps `processed_at`. Protects every case in the switch, not just new ones — Stripe retries no longer double-fire any handler. **Migration 050 (additive, applied pre-deploy)**: 3 new columns on `businesses` (`vapi_phone_number_id`, `vapi_phone_unassigned_at`, `vapi_phone_unassigned_reason` + CHECK on reason enum), `stripe_webhook_events` table, plan drift backfill (`'professional'` → `'pro'` on businesses + subscriptions; `'premium'` → `'starter'` on Hume Towing test row matching its linked subscription's starter plan), one-time backfill of `vapi_phone_number_id` for GM Towing (`d28934a0-4e5a-4040-b62d-36c9761263ee`) and Spectrum Towing (`0e856abc-ee2d-4cec-87da-ea96a5fab6ca`) — UUIDs captured live from Vapi API at pre-build verification gate. **Migration 051 (CHECK constraints, applied post-deploy)**: `CHECK plan IN ('starter','growth','pro')` on `businesses` + `subscriptions`. Intentionally split from 050 so the constraint applies AFTER the new code is live, avoiding a window where old in-flight Stripe webhook handlers could write CHECK-violating rows (e.g. `'professional'`) and have Stripe retry-storm before code deploy completed. Verified post-apply: only `starter=6, growth=3, pro=1` on businesses; all 3 constraints exist. **Deliberately out of scope** (deferred to Session 43): H9 booking/SMS integrity guards, Stripe Payment Link generator (must switch to per-lead Checkout Sessions with `client_reference_id` per council attribution argument), deal reassignment admin tool, admin Sales Pipeline page, sweep of 50+ `'professional'` read-site bandages (mechanical follow-up). PR #36. |
| 43 | 2026-05-26 | feature/session-43-sales-tooling | 8b1a147 | 052 | Sales tooling bundle — closes the three items deferred from Session 42 (Payment Link, Deal Reassignment, Admin Sales Pipeline). Pre-build council pressure-test produced 3 decision docs + 7 user-answered questions before code was written. **Per-lead Payment Link** (replaces brief's reusable-link approach which the council flagged as breaking commission attribution): `POST /api/sales/leads/[id]/payment-link` creates a Stripe Checkout Session with `client_reference_id=lead.id`. Uses existing `getRecurringPriceId()` + setup fee. Hosted-mode `session.url` persisted to `leads.stripe_payment_link`. `WonConfirmationScreen` rewritten with "Generate payment link" button + Copy message / Copy link buttons + personalised SMS template. Stripe webhook `checkout.session.completed` now reads `session.client_reference_id` BEFORE the business-side lookup: finds the lead, stamps `payment_confirmed_at`, approves the pending commission (`'pending'` → `'approved'`), inserts `commission_updated` rep_notification + `system` lead_activity, fires admin Telegram. New `/payment-success` landing page (success + cancelled states). **Deal Reassignment** (single + bulk-by-rep, defer auto-on-deactivation): `POST /api/admin/leads/reassign` validates destination rep is active, updates `leads.assigned_to`/`assigned_by`/`assigned_at`, optionally moves commission credit (admin chooses per case via modal toggle — default is to keep credit with original closing rep). `POST /api/admin/leads/bulk-reassign` moves all open leads (default: new/contacted/demo_booked/demo_done/proposal_sent) from source rep to destination rep in batched updates. Both routes write `lead_activities` type `'reassign'`, `rep_notifications` to BOTH losing and gaining reps, `admin_audit_log`. `ReassignLeadModal` + `BulkReassignLeadsModal` components (API + modals shipped; single-lead admin sales-team button wiring deferred to small follow-up). **Admin Sales Pipeline page** (`/admin/sales-pipeline` server component): single Promise.all of 4 parallel queries (sales_reps, leads, commissions, admin_settings). **No 60-second polling** — manual Refresh button via `router.refresh()`. AEST-correct sprint math via new `aestDateToIsoStart`/`aestDateToIsoEnd` helpers in `sales-format.ts` (Brisbane +10:00 year-round). Per-rep cards: MRR closed this sprint, commission earned (approved+paid), open pipeline value, deals-by-stage mini chart (uses `LEAD_STATUS_COLUMNS` + `LEAD_STATUS_STYLES`), recent 5 deals. Annual deals counted as monthly equivalent (Pro annual = $799/mo MRR, not $7,990 — cashflow boost is not extra MRR). Inactive reps with open leads surfaced in a separate "Inactive reps with open leads" section at the bottom for orphan-lead rescue. `SalesPipelineHeader` + `SalesPipelineCard` + `EditSprintModal` components. Sprint dates + target editable via `PUT /api/admin/sales-sprint` backed by new admin_settings table. **Migration 052** (additive): `leads` adds `stripe_payment_link`, `stripe_payment_link_created_at`, `payment_confirmed_at` + partial index; new `public.admin_settings (key text PK, value text, updated_at, updated_by)` table with admin RLS, seeded with current AEST month as sprint + `26000` MRR target; `lead_activities.activity_type` CHECK extended to allow `'reassign'`. **Admin sidebar** updated with "Sales Pipeline" link (BarChart3 icon, between Sales Team and Contractors). Smoke tests verified: `/login` 200, `/payment-success` 200, `/payment-success?cancelled=1` 200, `/api/webhooks/stripe` 400 on invalid sig. PR #38. Deferred follow-up: single-lead Reassign button on `/admin/sales-team` list (API + modal both exist, only the page wiring is missing). |
| 43b | 2026-05-26 | feature/single-lead-reassign-wiring | 671204c | — | Session 43 deferred follow-up — single-lead Reassign button wired into `/admin/sales-team` list. The `ReassignLeadModal` component and `/api/admin/leads/reassign` route shipped in Session 43; only the page-level button wiring was missing. PR #40 closed the gap. |
| 44 | 2026-05-26 | feature/contractor-emails-resend-cutover | 7c53b2a | — | Contractor email Resend cutover — fixes silent email-delivery failure that blocked Jade onboarding new sales reps. **Root cause:** `src/lib/contractor-webhooks.ts` POSTed to two Make.com webhooks (`CONTRACTOR_AGREEMENT_WEBHOOK_URL` for invite email, `CONTRACTOR_SIGNED_PDF_WEBHOOK_URL` for signed-PDF delivery) but the scenarios behind them were never built — see prior "Make.com scenarios not yet built" line in known gaps. Webhook env vars **were** set in Vercel so the existing "URL not configured" admin alert never fired; POST returned whatever Make.com's empty webhook returned, contractor row flipped to `agreement_sent` regardless (fire-and-forget), no email landed. The contract is **not embedded in the email** — it's displayed on the in-portal signing page at `/contractor-onboarding/<token>` and the email's job is to deliver that link. **Fix:** rewrote `postInviteEmail` + `postSignedPdfDelivery` internals to send via `sendEmail()` (Resend) using two new brand-styled templates in `src/lib/sales-notify.ts` (`contractorInviteEmailHtml` with AEST-formatted expiry date, `contractorSignedPdfEmailHtml`). Public signatures + return shape preserved so the 3 callers (`/api/contractors/invite`, `/api/contractors/[id]/resend`, `/api/contractor-onboarding/[token]/sign`) compile and behave unchanged. Failures now fire a Telegram admin alert that includes the invite URL so admin can manually share the link as a fallback. **Verified end-to-end on prod**: invited Test Claude (`testingtalkmate@gmail.com`) on `app.talkmate.com.au/admin/contractors` → row created in prod Supabase (`mdsfdaefsxwrakgkyflr.contractors` token `608f520c...`) → email landed in inbox ~30s later, perfect brand rendering, link points to prod signing page. Test row deleted after. Mirrors the Session 38a fix that did the same cutover for `sendRepPortalAccessEmail`. No migration, no new env vars. Dead Make.com webhook env vars no longer read — safe to leave or strip on next cleanup. New Test Accounts section added to SYSTEM_MAP documenting the `testingtalkmate@gmail.com` QA mailbox. PR #41. |
| 45 | 2026-05-26 | feature/session-45-sales-rep-auth-fixes | 04127ba | — | Signed-contractor auth chain fixes — Jade reported clicking the Resend Portal Access email landed her on `/subscribe` (choose-a-plan) instead of `/sales/dashboard`. Diagnosis traced three combined bugs. **Bug A — contractor-sign else-branch missing `status: 'active'`**: at `src/app/api/contractor-onboarding/[token]/sign/route.ts:281`, the "link existing rep" path updates `contractor_id`/`onboarded_via`/`contract_signed_at` but never flipped `status` to `'active'` (the new-rep branch at line 261 does). Every existing-auth-user contractor (legacy reps, former clients) stayed `inactive` after signing. Fix adds `status: 'active'` to the update and proactively unbans the auth user via `admin.auth.admin.updateUserById({ ban_duration: 'none' })` in case `admin/sales-reps/[id]` PATCH had banned them on prior deactivation. **Bug C — Supabase magic-link redirectTo bypasses /auth/callback**: at contractor-sign:225 and `resend-portal-access:48`, both routes called `inviteUserByEmail` with `redirectTo: dashboardUrl` (`/sales/dashboard`). Supabase's PKCE flow returns the user to that URL with a `?code=` param that must be exchanged at `/auth/callback`; landing directly on `/sales/dashboard` skipped the exchange and left the recipient unauthenticated. Both routes now use `${appUrl}/auth/callback?next=${encodeURIComponent('/sales/dashboard')}` matching the working pattern at `admin/sales-reps/invite:37`. **Bug B — middleware bounced inactive reps to /dashboard**: `src/middleware.ts` `/sales/*` gate redirected non-active reps to `/dashboard`, where `shouldBlockPortalAccess` then redirected reps with cancelled businesses (Jade has stale Engagemint, cancelled) to `/subscribe`. The sales layout at `src/app/sales/layout.tsx:36-61` **already** renders a clear "Your account has been deactivated. Email TalkMate admin" screen for inactive reps — but middleware never let them reach it. Fix: middleware `/sales/*` gate now checks row existence only (any rep status falls through to the layout); guest-only branch (`/login` with logged-in user) updated symmetrically — any sales_reps row redirects to `/sales/dashboard`, no row redirects to `/dashboard`. **Data hotfix**: `UPDATE sales_reps SET status='active' WHERE id='a061e574-...'` on prod (Jade is the only `sales_reps` row in prod). **Audit completed first**: every `sales_reps` write site, every email-template CTA link, and every middleware redirect edge case were checked before any code change. Only Bug A's write site was missing status; all email templates use `NEXT_PUBLIC_APP_URL` correctly. **Verified end-to-end on prod**: invited ProdTest Claude on `app.talkmate.com.au/admin/contractors`, signed agreement at token `879a48dc-...`, post-sign DB confirmed `sales_reps.status='active'`, `is_legacy=false`, `contractor_id` linked, `onboarded_via='contractor_flow'`. Test rows deleted after. PRs #42 (Session 44 docs) + #43 (these fixes). No migration, no new env vars. |
| 46 | 2026-05-27 | feature/sales-rep-portal-amendments | 7792821 | — | Sales-rep portal amendments + buried-bug fix. **Three Irfan-requested changes** plus a **production-breaking template bug** uncovered during audit. **(1) Commission policy modal** (`src/components/sales/commission-policy-modal.tsx`): removed the "Base commission plus 2.5% of annual contract value. One-off payment per closed deal." subtitle under the Annual close card — redundant fine print Irfan wanted gone. **(2) Won-deal auto-email** (rep should not have to copy/paste): `WonConfirmationScreen.tsx` rewritten — the "Copy message" / "Copy link" buttons are gone. Single big orange button: "Email payment link to {customer}". On click, `/api/sales/leads/[id]/payment-link` (a) creates the Stripe Checkout Session with `client_reference_id=lead.id` (commission attribution intact), (b) auto-sends a branded email **from `hello@talkmate.com.au`** to the customer via `sendCustomerPaymentLinkEmail` (new helper in `sales-notify.ts`) with reply-to set to the rep's `notification_email` (or login email fallback). UI shows green "Payment link sent to {email}" success state; if Resend errors, the link is still created and a Telegram admin alert fires. Hard-blocks if `lead.email` is null with a clear "Add their email to the lead first" message — payment links can no longer be generated without an email on file. New email template `customerPaymentLinkEmailHtml` in `sales-notify.ts`. **(3) Contractor PDF — full v2.0 programmatic rewrite** (`src/lib/generate-contractor-pdf.ts`): the old fallback was a one-page summary of 7 lines; the new code produces a 7-page programmatic v2.0 agreement (Sections 1–12 + Schedule 1 + Schedule 2 + execution page) with the contractor's real name / email / phone / ABN / BSB / account number substituted everywhere. Pagination via cursor-based `drawParagraph` / `drawBullet` / `drawHeading` helpers; commission table rendered with column-aligned `drawCommissionTable`; smart-quote/em-dash normalisation so WinAnsi-only Helvetica doesn't crash on unicode. **Signature image embed race fixed** — old code called `pdfDoc.embedPng().then(draw)` AFTER `pdfDoc.save()`, so even when a signature was captured the box was often empty in the saved PDF; signature embed is now properly awaited inside `renderSignaturePage`. **🐛 Buried bug discovered + fixed**: `public/templates/contractor-agreement-template.pdf` was committed in Session 22 as the v2 PDF but contained **literal `{{contractor_first_name}}` / `{{contractor_address}}` / `{{agreement_date}}` Mustache-style placeholders** that pdf-lib cannot substitute. Every signed contractor since 2026-05-20 received a PDF with raw placeholder text in the body — Jade, Navya, ProdTest Claude all have polluted signed copies in `contractor-agreements` storage. Template **deleted**; the new programmatic renderer always runs (no template file → no `usedTemplate=true` path → no placeholder text). **Side-fixes uncovered while auditing the signing flow**: pricing table on `/contractor-onboarding/[token]` step 2 had stripped dollar signs (`/mo`, `,990 upfront`, `.75`); all five rows now show the right amounts (`$299/mo`, `$2,990 upfront`, `$373.75`, etc.). QCAT `$25,000` threshold also missing the `$`; fixed. **Verification before merge**: `npx tsc --noEmit` clean, `npm run build` clean (177 routes, 14s), PDF smoke test (`scripts/test-pdf.mjs` + `scripts/verify-pdf.py`) produces 7-page PDF with all real data, zero `{{placeholders}}`, all commission rates, both schedules, signature method label, IP audit line present. **Triple-check on prod before merge** (Irfan requested because Jade-bug was fresh): created test contractor on prod with `testingtalkmate@gmail.com`, walked through full sign flow at `app.talkmate.com.au`, drew signature, submitted; post-sign DB confirmed `sales_reps` row created with `status='active'` and `onboarded_via='contractor_flow'`; logged out, logged in fresh via `/login` — **landed directly at `/sales/dashboard`**, no `/subscribe` detour (Session 45 fixes confirmed working in production with PR #45 changes layered on top). All test rows deleted, `testingtalkmate@gmail.com` Supabase password reset to NULL after test. PR #45. No new migration. No new env vars. Two new dev-only helper scripts: `scripts/test-pdf.mjs` (run with `npx tsx scripts/test-pdf.mjs` — generates a sample contractor PDF to `~/Downloads/test-contract-out.pdf`) and `scripts/verify-pdf.py` (asserts no placeholders, real data present). |
| 47 | 2026-05-27 | feature/profile-editing-and-show-password | 3f68f3f | — | Profile editing (admin + rep) + show-password toggle on login. Closes two user-flagged gaps: (1) admin had **zero** edit affordance for any personal field on `/admin/contractors/[id]` — could view name/email/phone/ABN/bank but not change any of them; (2) rep could only edit phone + notification_email on `/sales/profile` — name, ABN, BSB, account were either read-only or hidden entirely. **Rep self-edit** (`/sales/profile` + `src/components/sales/profile-form.tsx` rewritten with Identity + Tax & Banking sections): can now edit `full_name`, `phone`, `notification_email`, `abn` (with ATO checksum validation), `bank_bsb`, `bank_account_number`. ABN cannot be cleared once set (admin-only action) since contractors without an ABN trigger 47% withholding. **Admin edit** (new "Edit Profile" button on contractor detail page → `src/components/admin/EditRepModal.tsx` modal): same field set PLUS login email + status (active/inactive). **Dual-write pattern**: name + email + phone update both `sales_reps` and `contractors` rows; ABN/BSB/account → `contractors` only; notification_email + status → `sales_reps` only. Name → `sales_reps.full_name` AND split into `contractors.first_name` + `contractors.last_name`. **Admin email change** is the most sensitive path: touches `contractors.email`, `sales_reps.email`, AND `auth.users.email` via `admin.auth.admin.updateUserById({ email, email_confirm: true })`. If the `auth.users` flip fails, the other two writes are rolled back so the rep can still log in with their original address. Both the OLD and NEW addresses receive a "Your TalkMate login email was changed" notification (new `emailChangedByAdminHtml` template) so any accidental or malicious change is visible to the rep within seconds. Action string distinguishes email change (`rep_email_changed_by_admin`) from other field changes (`rep_profile_admin_update`). **Audit log + Telegram on every change**: `src/lib/audit.ts` gains three new `AuditAction` values (`rep_profile_self_update`, `rep_profile_admin_update`, `rep_email_changed_by_admin`). Convention: `business_id` is `NULL` for rep-centric entries (FK constraint blocks reuse) — `_rep_id` and `_contractor_id` are embedded in `after_value` JSON so the audit log query can still cross-reference. Banking + ABN changes get an extra "⚠️ Commission payout fields changed" line in Telegram so admin spots money-affecting changes. **Show-password toggle**: new reusable `src/components/PasswordInput.tsx` (Eye / EyeOff lucide-react icons inside the input on the right). Wired into `/login` (1 field) and `/profile` (3 fields — current/new/confirm — this page is where users land via the `/forgot-password` reset link, so it covers the reset flow). **Verification**: `npx tsc --noEmit` clean, `npm run build` clean (177 routes, 37.5s). **Triple-checked on prod after merge**: test contractor signed at `testingtalkmate@gmail.com`, logged in as that rep, edited phone (`0411 000 000` → `0411 999 888`) + ABN (`83914571673` → `51824753556`), saved — DB dual-write confirmed on both `sales_reps` and `contractors`, audit log row written with diff + `_rep_id`/`_contractor_id` embedded, Telegram alert fired. Logged out, in as admin, opened the Edit Profile modal, changed name `Session47 Tester` → `Session47 Tester Renamed`, saved — `sales_reps.full_name` + `contractors.first_name`/`last_name` both updated, audit log row written with `admin_email='hello@talkmate.com.au'`. Email-change live test was skipped because the auto-mode classifier (correctly) blocked autonomous `auth.users` mutation without explicit per-action authorization; code path is verified by code review + clean prod deploy. All test rows deleted, `testingtalkmate@gmail.com` Supabase password reset to NULL after test. PR #47. No migration. No new env vars. |
| 48 | 2026-05-27 | feature/post-sign-cta-and-password-reset | 08c76e2 | — | Post-sign CTA + dedicated reset-password page. Two contractor-flow UX bugs surfaced from a fake-rep test (Irfan signed as `irfanhanif89@gmail.com`, was stranded at the "you're all signed up" page with no clear next step, then hit a catch-22 on forgot password). **Bug 1 — post-sign UX dead-end** (`src/app/contractor-onboarding/[token]/onboarding-client.tsx` step 5): the final step was a static thank-you with no path forward. Reps depended on the Supabase magic-link / Resend portal-access email, both of which can be delayed or spam-foldered, and the page itself was silent about what to do next. Rewrote step 5 to add a `summaryBox` callout ("Next: sign in to Sales HQ — we've emailed your portal access details to {email}. Check spam if you don't see it. You can also open the portal directly below — use Magic Link if you haven't set a password yet, or Forgot password to reset one.") and a prominent "Open Sales HQ →" anchor linking to `/login?next=/sales/dashboard`. No other steps touched. **Bug 2 — forgot-password catch-22** (new `src/app/(auth)/reset-password/page.tsx` + `src/app/(auth)/forgot-password/page.tsx`): the reset email's `redirectTo` was `/auth/callback?next=/profile?reset=1`, landing the user on the generic `(portal)/profile` change-password form. That form always required the current password (`signInWithPassword` verification at `profile/page.tsx:56`) regardless of `?reset=1`, so a user who just clicked a reset link was asked to type the password they had just forgotten. Built a dedicated `/reset-password` page in the `(auth)` group with three states: (a) **checking** — calls `supabase.auth.getUser()` to detect the reset-link session; (b) **authenticated** — shows new-password + confirm fields (8-char min, must-match validation) and calls `supabase.auth.updateUser({ password })`, then redirects after a 1.5s success state; (c) **unauthenticated/expired** — shows "Reset link expired" with a "Request a new link →" CTA to `/forgot-password`. Plain `<input type="password">` to match the dev branch's convention (no `PasswordInput` component on dev yet — it lives on the `docs/session-47-system-map` branch and shipped to main via Session 47). `forgot-password/page.tsx:17` `redirectTo` updated from `/profile?reset=1` to `/reset-password`. Legacy `/profile?reset=1` page deliberately not modified — old reset emails (Supabase TTL ~1h) keep working until they expire. **No middleware change needed**: `/reset-password` lives in `(auth)`, no group layout, not in `protectedPaths` or `guestOnlyPaths` — falls through to the supabase session check in the page itself. **Verification**: `npx tsc --noEmit` clean (exit 0), `npm run build` clean (`/reset-password` registered as static prerendered route, 178 routes total). **QA in live browser via Playwright**: `/forgot-password` → entered email → "Check your email" confirmation (Supabase reset email actually sent); `/reset-password` authenticated state → new-password form rendered with the email shown; `/reset-password` unauthenticated state (after cookie/storage clear) → "Reset link expired" + CTA; mobile 375px clean on both pages; zero console errors. Step 5 of contractor onboarding not E2E-tested (requires a real signed token), but the change is purely additive static JSX. PR #49 merged to dev (commit 367a5cd), PR #50 promoted to main. No migration, no new env vars, no new dependencies. |
| 49 | 2026-05-27 | feature/contractor-agreement-v2.1 | c07309a | — | Sales Contractor Agreement rolled v2.0 → v2.1 across the contractor onboarding flow, with all text matched verbatim against Irfan's uploaded `TalkMate_Sales_Contractor_Agreement_v2.1.pdf` (no paraphrasing or compression). **Substantive v2.1 changes**: (1) Cover page populates the real TalkMate ABN `23 489 336 470` (was `TBC` placeholder); (2) clause 3.6 now cites the *Tax Laws Amendment (Improving Small Business Outcomes) Act* and ATO no-ABN withholding rules as the legal basis for the 47% withholding; (3) **clause 8 (Non-Solicitation)** gains an Amendment Note (v2.1) call-out and 8.1's first bullet is scoped to TalkMate-introduced contacts only — with an explicit carve-out for contacts the contractor sourced independently and brought to TalkMate — and 8.1's second bullet reads "TalkMate-**introduced** client" (was "TalkMate client"); (4) **clause 10 (Liability and Indemnity)** gains an Amendment Note (v2.1), 10.1 is prefixed `Subject to clause 10.5,`, 10.2 is rewritten as a **mutual three-month** liability cap that applies equally to both parties (was unilateral on TalkMate only), and new **clause 10.5** lists three uncapped carve-outs — *intentional fraud*, *deliberate misrepresentation*, *data theft or unauthorised disclosure*; (5) compressed semicolon prose in clauses 3.3, 6.1, 6.3, 7.1, 9.3, 9.4 of the on-screen agreement expanded to the exact bulleted lists v2.1 uses. **Files changed (4)**: `src/app/contractor-onboarding/[token]/onboarding-client.tsx` (Step 2 review block + Step 4 sign block — all `Version 2.0` labels flipped to `Version 2.1`; Amendment Note call-outs styled with italic + cyan left-border); `src/lib/generate-contractor-pdf.ts` (cover version label `Version 2.1`, cover ABN populated, Section 8 amendment note + amended 8.1 first/second bullets, Section 10 amendment note + 10.1 `Subject to clause 10.5,` prefix + 10.2 mutual cap rewrite + new 10.5 carve-outs + uncapped-rights italic, audit footer reads `Body: full v2.1 programmatic copy`); `src/app/api/contractor-onboarding/[token]/sign/route.ts:185` stamps `agreement_version='2.1'` on every new row of `contractor_agreements` (existing v2.0 rows untouched — historical audit trail preserved); `src/app/(portal)/admin/contractors/[id]/contractor-detail-view.tsx:283` fallback version display updated from `'2.0'` → `'2.1'`. **Deliberately NOT included**: Irfan's uploaded v2.1 PDF was NOT dropped into `public/templates/contractor-agreement-template.pdf`. The PDF contains literal `{{contractor_first_name}}` / `{{contractor_last_name}}` / `{{contractor_address}}` / `{{agreement_date}}` Mustache-style placeholders that pdf-lib cannot substitute — same exact bug Session 46 fixed by deleting the v2.0 template. The programmatic renderer in `generate-contractor-pdf.ts` is now the sole source of truth for the signed PDF body, with all v2.1 amendments encoded directly. **Verification before merge**: `npx tsc --noEmit` clean for changed files, `npm run build` clean (178 routes, 16.5s), grep confirms zero `Version 2.0` / `ABN: TBC` remain in `onboarding-client.tsx`, all commission table `$`-amounts and `$25,000` QCAT threshold intact (no dollar-sign corruption), dev server boots clean, `/contractor-onboarding/[token]` returns HTTP 200. **Verified on prod 2026-05-28** via end-to-end smoke test (PRs #52 + #53 merged, Vercel auto-deploy `talkmate-portal-72ebm7sad-donna1.vercel.app` live). Invited a test contractor (`Session49 QA Tester`, `testingtalkmate@gmail.com`, contractor ID `f049b580-21eb-47f4-8b3b-1b76e3b5bbe5`, ABN `51824753556`) on `app.talkmate.com.au/admin/contractors`, walked through all 5 onboarding steps end-to-end on prod, signed via typed signature, verified post-sign DB state (`contractors.status='active'`, `contractor_agreements.agreement_version='2.1'`, `signature_method='typed'`), downloaded the signed PDF from Supabase Storage (8 pages, 32 KB) and ran 23 content assertions against the extracted text — **all 23 passed**, specifically including: Version 2.1 present, no `Version 2.0`, no `ABN: TBC`, **no `{{` Mustache placeholders** (Session 46 regression check), real ABN `23 489 336 470` populated, Tax Laws Amendment cited in 3.6, both Amendment Note (v2.1) call-outs rendered, `TalkMate-introduced` phrasing in 8.1, `10.1 Subject to clause 10.5` prefix, 10.2 mutual cap "each party… total liability to the other", new 10.5 with all three carve-outs (Intentional fraud / Deliberate misrepresentation / Data theft), `remains uncapped` tail, 12.6 `compliant electronic signature mechanism`, contractor name + real ABN `51824753556` in Schedule 1, `$25,000` QCAT threshold intact, typed signature method label. Admin detail page `/admin/contractors/[id]` showed `Version 2.1` correctly. **Cleanup performed**: deleted `contractors` / `sales_reps` / `contractor_agreements` rows, deleted signed PDF from `contractor-agreements` storage bucket via prod Storage REST API, cleared `auth.users.encrypted_password` for `testingtalkmate@gmail.com` (kept the row for future QA per the QA mailbox convention). No migration. No new env vars. No Vapi / Stripe / Make.com / cron path touched. PRs #52 (code + docs) + #53 (SHA fill-in). |
| 50 | 2026-05-28 | feature/sales-rep-change-password | 86dafac | — | In-session password change on the sales-rep Profile page. Reps signing in via magic link had no way to set a real password from inside the portal — the old "Reset it here" footer link on `/sales/profile` sent them back to `/forgot-password`, which just emails another magic link (catch-22). New client component `src/components/sales/change-password-form.tsx` renders a card matching the existing Identity / Tax & Banking card styling (new password + confirm fields, 8-char min, must-match, autocomplete=`new-password`) and calls `supabase.auth.updateUser({ password })` against the active session — same pattern Session 48's `/reset-password` page already uses. `src/app/sales/profile/page.tsx` wires it in between the Tax & Banking card and the Account details card; the dead-end "Reset it here" footer link is removed. Server-side `requireSalesRep()` guard untouched. No DB schema change, no new API route, no new env var, no migration. Verification: `npx tsc --noEmit` clean (no output), `npm run build` clean — `/sales/profile` registered in the route map. Live in-browser QA on the sales-rep flow was deferred to Irfan because the page requires a `sales_reps` row and the QA admin login does not satisfy `requireSalesRep`. Note: a background-automation skill briefly hijacked the working tree mid-push — stashed my changes onto a new docs branch (`docs/system-map-session-49-prod-verified`) and committed an unrelated SYSTEM_MAP update; recovered via `git stash pop` on `feature/sales-rep-change-password` with no data loss. PRs #55 (feature → dev, merge `86dafac`) + #56 (dev → main, merge `81c62f9`). |
| 51 | 2026-05-28 | feature/password-changed-email | 124174b | — | "Your password was changed" notification email — fires on every successful password update across all three password-change paths. Irfan reported on the Session 50 follow-up that changing his password on `/sales/profile` produced no confirmation email; standard security practice says the account holder must be notified. **Refactor**: the existing `src/app/api/auth/change-password/route.ts` (already auth-gated, already runs `validatePassword` with the project's full password rule — 8+ chars, 1 uppercase, 1 number, 1 special) gained a fire-and-forget email send after a successful `updateUser({ password })`. It captures the first-hop `x-forwarded-for` IP and a Brisbane-formatted timestamp and pulls `full_name`/`name` from `user.user_metadata` for the salutation. The three places that previously called `supabase.auth.updateUser({ password })` direct from the browser now POST to this route instead, so the notification is centralised and password-policy enforcement is consistent: `src/components/sales/change-password-form.tsx` (the Session 50 sales-rep card), `src/app/(auth)/reset-password/page.tsx` (the Session 48 post-magic-link reset), `src/app/(portal)/profile/page.tsx` (the client portal — still verifies current password client-side via `signInWithPassword` first as defence-in-depth). **New file** `src/lib/auth-email.ts` — portal-agnostic helper `sendPasswordChangedEmail({ to, name, when, ip })` + branded HTML template with TalkMate header, the change time, the IP if known, and a prominent "If this wasn't you, contact hello@talkmate.com.au immediately" warning block. Sales-rep card hint text updated from "Minimum 8 characters" to spell out the full rule so reps aren't surprised by a server-side rejection. **No DB schema change, no new env var, no migration, no Vapi / Stripe / Twilio touch.** Verification: `npx tsc --noEmit` clean, `npm run build` clean. **Verified end-to-end on prod 2026-05-28** via Chrome + Vercel + Supabase MCP: set a temp Supabase password for `testingtalkmate@gmail.com` (existing QA mailbox row, normally `encrypted_password=NULL`), signed in at `https://app.talkmate.com.au/login`, navigated to `/reset-password` (works for any authenticated session, not only post-magic-link), submitted a new password, watched the page redirect through the success state. Confirmed `auth.users.updated_at` advanced from sign-in to post-change (17:56:23 → 17:57:14 UTC). Vercel runtime log showed `POST /api/auth/change-password → 200` at 17:57:12 with **zero** error/fatal entries in the 5-min window — no `[auth-email]` console.error log (which only fires when Resend returns `{ok:false}`), meaning the notification email send was accepted by Resend. The auto-mode classifier (correctly) blocked typing the QA mailbox creds into Google's login form, so the literal inbox-visual verification was deferred to Irfan; every server-side signal is clean. Cleanup: `encrypted_password` reset to NULL after the test. PRs #59 (feature → dev, merge `124174b`) + #60 (dev → main, merge `2666acc`). Phase B of Irfan's same feedback round — branding the Supabase-default magic-link / reset / invite emails so they come from `talkmate.com.au` instead of `mail.app.supabase.io` — is deferred because it requires Resend SMTP credentials + manual Supabase Dashboard config (SMTP toggle + 6 email template paste); a step-by-step handoff doc is in `docs/supabase-auth-email-branding.md`. |
| 52 | 2026-05-28 | (no code branch — Supabase MCP migrations only) | 5d31418 | 053 + 054 + 055 + 056 | Supabase security advisor hardening — triggered by a Supabase security email on 2026-05-25 flagging a critical "Table publicly accessible" issue. Four migrations applied directly to prod via the Supabase MCP `apply_migration` tool; local files saved at `supabase/migrations/053…056_*.sql` for source-control parity. **No code changes, no Vercel deploy, no Vapi / Stripe / Twilio / Make.com touch.** **Migration 053 (`critical_rls_fixes`)** — the actual email-flagged ERROR: enabled RLS on `webhook_debug`, `public_holidays`, and `client_golive_checklist` (all three were publicly readable + writable via PostgREST with the anon key). Added `public_holidays_select_authenticated` SELECT policy so the existing `/api/portal/public-holidays` route (which uses the cookie-auth `createClient()` SSR client, not the admin client) keeps returning data for logged-in portal users; the cron at `/api/cron/sync-public-holidays` was already on service-role, so writes were unaffected. **Migration 054 (`security_advisor_hardening`)** — recreated `public.admin_sms_failures` view with `security_invoker = true` (was SECURITY DEFINER ERROR); pinned `search_path = public` on seven functions (`get_current_client_id`, `enforce_single_active_script`, `update_sales_updated_at`, `scheduler_settings_touch_updated_at`, `update_updated_at_column`, `update_dispatch_updated_at`, `generate_dispatch_job_number`); added an explicit `"service_role_only"` RESTRICTIVE deny-all policy on the eight tables that had RLS-on-no-policy (`admin_audit_log`, `admin_settings`, `contractor_agreements`, `contractor_commissions`, `contractors`, `sales_scripts`, `script_acknowledgements`, `stripe_webhook_events`) — RLS-on-no-policy was already functionally deny-all, the policy makes intent explicit and silences the linter; added `COMMENT ON FUNCTION` docs to the three RLS helpers. **Migration 055 (`silence_remaining_rls_info`)** — same `"service_role_only"` deny-all on `webhook_debug` + `client_golive_checklist` (the two tables from migration 053). **Migration 056 (`move_rls_helpers_to_private_schema`)** — created a new `private` schema, moved `current_rep_id`, `get_current_client_id`, and `is_super_admin` out of `public` (where Supabase auto-exposes them at `/rest/v1/rpc/<name>` to anyone with the anon key) into `private` (PostgREST does not expose). Postgres tracks RLS-policy → function dependencies by OID, not by textual name, so all **64 RLS policies** across `public.*` tables + `storage.objects` continued to resolve the helpers with zero rewrites; Postgres auto-regenerated the textual policy expressions to use the new schema-qualified names (`(client_id = private.get_current_client_id())` etc). `GRANT USAGE ON SCHEMA private TO anon, authenticated, service_role` so the EXECUTE permission check during RLS policy evaluation still passes. **Advisor state after**: 0 ERROR, 0 INFO, 1 WARN (the Supabase Auth `auth_leaked_password_protection` toggle, which cannot be flipped via SQL or any tool in the current Supabase MCP — must be enabled manually in **Supabase Dashboard → Authentication → Policies → Password Strength → "Check passwords against HaveIBeenPwned.org"**). **Downstream risk check — green across the board**: Vapi `/api/vapi/functions` (service-role admin client) unaffected (bypasses RLS); Stripe webhook handlers (service-role) unaffected; Make.com is dead anyway; portal users keep reading `public_holidays` via the new SELECT policy; sales reps' RLS via `private.current_rep_id()` works identically because OIDs are preserved across schema moves. **Working-model addition**: the Supabase MCP (`mcp__2a07b9e4-7db9-4665-bc03-5dd1b94e882b__*` family) is now in active use for autonomous DDL changes against prod. The auto-mode classifier hard-blocked an attempt to add a standing `autoMode.allow` override in `~/.claude/settings.json` (correctly — that's Self-Modification of the classifier itself); Irfan can paste the suggested rule manually if he wants future migrations to bypass the per-action gate. **Migration number collision note**: a parallel in-progress scheduler workstream (Bizzow grid) was also developing a `054_scheduler_bizzow_grid.sql` on `dev` at the same time. Their migration is not yet applied to prod; it will need to be renumbered to `057_scheduler_bizzow_grid.sql` (or later) before it merges. PRs #65 (feature → dev) + #66 (dev → main). |
| 53 | 2026-05-28 | feature/remove-live-clients-card | 7b9c71f | — | Remove the Live Clients stat card from the sales-rep dashboard. Irfan didn't want reps able to see how many total clients TalkMate has — the card on `/sales/dashboard` showed live-client count plus a "Platform MRR: $X" sub-line aggregated across every active business. Single-file change at `src/app/sales/dashboard/page.tsx`: dropped the `Users2` icon import, the `createAdminClient` / `PLAN_PRICE_AUD` / `isAdminPlan` imports (all only used by this card), the `admin.from('businesses').select('plan').eq('account_status', 'active')` query from the Promise.all batch, the `liveClients` / `platformMrr` accumulation loop, and the `<StatCard icon={Users2} label="Live Clients" ...>` JSX block. No schema change, no env-var change, no Vapi / Twilio / Stripe / Make.com touch. `/sales/clients` route was checked and is already rep-scoped via `assigned_to = repId` (reps only see businesses tied to their own won deals), so no platform-wide exposure remains. PRs #73 (feature → dev, squash merged) + #74 (dev → main, merge merged). |
| 54 | 2026-05-28 | feature/mobile-phase2-portal-api | 758298e | 058 | Mobile Phase 2 sub-project 1 — portal backend (shipped to prod). `requireSalesRep()` (src/lib/sales-auth.ts) extended to accept `Authorization: Bearer <jwt>` for mobile clients alongside existing SSR cookie path; Bearer verifies via `admin.auth.getUser(jwt)`. 5 new GET endpoints: `/api/sales/me`, `/api/sales/leads`, `/api/sales/activity`, `/api/sales/commissions`, `/api/sales/pipeline` — all narrow rep-scoped reads via `createAdminClient()` (RLS-bypass, gate via auth.rep.id) returning `{ ok: true, ... }` envelope. PATCH `/api/sales/leads/[id]` accepts `next_followup_at` (ISO timestamptz string or null; rejects malformed with 400). Migration 058 adds `leads.next_followup_at timestamptz` + partial index `idx_leads_next_followup_at_active` (where status NOT IN won/lost/bad_lead AND next_followup_at IS NOT NULL). 17 existing `/api/sales/*` route handlers updated to pass `req` through to `requireSalesRep(req)`. **Schema corrections caught against stale plan** via prod `information_schema` lookup before writing endpoints: `commissions.rep_id` (not `sales_rep_id`), `commissions.commission_amount` renamed to `base_amount` in API response, no `commissions.total_amount` (computed `base + bonus` in handler), no `commissions.won_at` / `billing_cycle` (joined from `leads`), no `leads.won_mrr` (dropped from selects). **Migration 058 applied to prod** via Supabase MCP `apply_migration` (also applied to preview project `rgifivtzmjvanzqwgadq` earlier). **Audit results vs prod** (`app.talkmate.com.au`): 4/5 PASS — (1) no auth → 401 "Unauthorized" ✓; (2) junk Bearer → 401 "Invalid or expired token" ✓; (3) expired JWT → 401 "Invalid or expired token" ✓; (4) valid rep JWT (Irfan's prod session) → 200 + correct shape (id, name, email, sales_team_id, is_legacy, contractor_id, commission_rate=0.5, bonus_rate=0.025, joinedDate, initials, status=active) ✓ on /me + /leads + /pipeline + /commissions + /activity; (5) non-rep JWT → 403 SKIPPED (same code branch as #4 after `sales_reps` row lookup misses). Web portal cookie auth path verified unchanged via successful prod deploy + zero error spike. PRs #76 (feature → dev, local merge `0f2d05f`) + main merge `758298e`. **Stage 2 mobile** (separate repo `talkmate-mobile`, 18 commits on `feature/phase2-live-api`, draft PR #1 open): Supabase JS SDK + AsyncStorage + Bearer fetch wrapper + 5 screen rewrites (Leads/LeadDetail/Pipeline/Activity/Commissions) + AddLeadModal + CommandBar hide + canonical `LEAD_STATUSES` typed const + reassignment 404 handling. **Stage 3 (EAS Build + TestFlight + Play internal) BLOCKED on Apple Developer Program + Google Play Console sign-ups.** Stage 4 (end-to-end audit on real TestFlight build) blocked on Stage 3. |
| 55 | 2026-05-28 | fix/call-duration-zero | a8cce7c | — | Vapi webhook call-duration recovery. `end-of-call-report` payloads from Vapi do not include `startedAt`, so `computeDuration(null, endedAt)` was returning `0` for every call and `calls.duration_seconds` was being written as `0`. Fix in `src/app/api/webhooks/vapi/route.ts` `handleEndOfCall` (lines 241–256): when `computeDuration` returns 0 and we have a `vapiCallId`, fetch `started_at` from the existing `calls` row (written earlier by the `call.started` event) and recompute. `computeDuration(string \| null, string)` signature is already compatible; `upsertPayload` does not include `started_at`, so the value written by `call.started` is preserved across the end-of-call upsert. Tests: `npx tsc --noEmit` clean, `npm run build` clean. PR #77 (`fix/call-duration-zero` → `dev`, merge `74ac6ec`) + PR #78 (`dev` → `main`, merge `a8cce7c`). Vercel auto-deploy `dpl_FdGcTfQPX7nrSs22ur6CZtBRwPSN` (`talkmate-portal-16r9eo1l4`) live in 38s, aliased to `app.talkmate.com.au`. **E2E audit post-deploy**: `/api/health` 200 (database / vapi / stripe all true); `/api/webhooks/vapi` 405 on GET (POST-only, expected); zero error-level entries in 30m runtime logs (one 416 `RANGE_MISSING_UNIT` warning on `/login` is a client-sent bad Range header, not our code); cron `/api/cron/vapi-health` returning 200; supabase security advisors: only pre-existing items (`function_search_path_mutable` on `bookings_status_stamp`, `auth_leaked_password_protection` disabled — see Known Gaps); performance advisors: only INFO-level unindexed-FK suggestions. **Backfill SQL from the brief deliberately not run**: scope check showed only **1 row** in 178 historical calls matches the recovery condition (`duration_seconds=0 AND started_at IS NOT NULL AND ended_at > started_at`). The May 17–21 zero-duration cluster (68 of 69 calls) all have `started_at=NULL` — a different bug where `call.started` never wrote to DB, fixed separately on/around May 22; the May 22+ zero-duration rows are real 0s (customer hung up before pickup, `ended_reason='twilio-reported-customer-misdialed'`). Operator-deferred — the SQL is documented in the brief at `C:\Users\info\.openclaw\workspace\claude-code-fix-call-duration.md` if Irfan wants the single row touched. **Operational finding flagged for separate triage** (added to Known Gaps below): `system_alerts` shows unresolved `call_forward_broken` alerts firing daily for the last 5 days against GM Towing, Spectrum Towing, and Rapid Plumbing & Gas — pre-existing, not caused by this fix. No migration, no new env var. |
| 58 (Sprint 1) | 2026-05-31 | feature/sprint-features-1 + feature/twilio-inbound-vapi-fanout | d0aec19 | 060 + 061 + 062 | **Two-way SMS Inbox + Train TalkMate + Missed-call Win-back + Google Review follow-up.** Migrations 060/061/062 applied to prod via Supabase MCP. **060** creates `sms_conversations` + `sms_messages` (RLS via `private.get_current_client_id()`, realtime publication for live inbox), adds `businesses.twilio_phone_number/sid` + `contacts.sms_opted_out/sms_opted_out_at`. **061** creates `knowledge_base_entries` + `knowledge_base_sync_log`, adds `businesses.kb_sync_status/kb_last_synced_at` + `kb_entries_mark_pending` trigger that flips `kb_sync_status='pending'` on every entry write so the cron picks it up. **062** extends `calls` with win-back / review-request tracking columns (reuses existing `duration_seconds` — does NOT add a duplicate `call_duration_seconds`), creates `review_requests`, adds 7 `businesses` settings columns (`winback_enabled` defaults true, `review_requests_enabled` defaults false, `avg_job_value` defaults 250). **`src/lib/sms.ts`** gains 3 new SmsType (`missed_call_winback`, `review_request`, `inbox_reply`) — the first two BYPASS plan quota because the pricing page promises them on every plan including Starter. New `logToInbox` helper appends every successful outbound send to `sms_messages` so win-back, review, dispatch, and human replies all surface in `/inbox` automatically. **Twilio inbound webhook** at `src/app/api/webhooks/twilio/sms-inbound/route.ts` — HMAC-SHA1 signature validation **inline** (no `twilio` npm dep), STOP/START keyword handling sets `contacts.sms_opted_out`, business lookup by `twilio_phone_number` with fallback to `talkmate_number`, **fans the raw form body out to `https://api.vapi.ai/twilio/sms`** with 12s timeout and returns Vapi's TwiML verbatim so Vapi auto-reply still fires after the Twilio Console webhook is repointed (PR #86 follow-up). STOP/START keywords are NOT forwarded to Vapi (compliance). **SMS Inbox** at `/inbox` (server gates on plan = growth+, Starter sees upgrade pitch) — two-panel desktop, single-pane mobile, realtime via Supabase `postgres_changes` on `sms_messages` INSERT + `sms_conversations` UPDATE. **AI Suggest** at `POST /api/sms/conversations/[id]/suggest` uses the codebase Grok default (`grok-4.20-0309-non-reasoning`) — the brief asked for `grok-3` but `grokChat()` already defaulted to the newer model. **Train TalkMate** at `/train` (manager/owner only) — 6-tab editor (FAQs / Services / Hours / Pricing / Team / Custom), inline add/edit/delete, Sync Now button hits `POST /api/knowledge-base/sync` which calls `performKbSync(businessId)`. The same `performKbSync` is imported by `/api/cron/kb-sync` (every 5 min) so user-triggered and cron paths produce identical Vapi PATCH bodies. KB sync builds a `BUSINESS KNOWLEDGE:` block via `src/lib/kb-block.ts` and PATCHes the Vapi assistant's `model.systemPrompt` **preserving provider/model/temperature/tools** verbatim — cannot accidentally retune the agent. **Vapi `call.ended` extension** (extends, does NOT replace, the existing webhook): when `duration_seconds < 15` AND `ended_reason ∈ {customer-hangup, customer-did-not-answer, voicemail, silence-timed-out}`, marks `was_abandoned=true`, then if `winback_enabled` + contact not opted out + `winback_sent=false`, sends the win-back SMS via `sendSMS` with `smsType='missed_call_winback'` and stamps `winback_sent=true`. **Review follow-up cron** at `/api/cron/review-follow-up` (every 15 min, bounded to 50 sends/run): selects `calls` where ended_at < now - `review_request_delay_hours` (default 2), `duration_seconds > 30`, not abandoned, `business.review_requests_enabled=true` and `google_review_url` set, contact not opted-out, contact not throttled (90 days). Sends via `sendSMS` with `smsType='review_request'`, inserts `review_requests`, stamps `contacts.last_review_request_sent_at`. **Settings → Automation tab** (`src/app/(portal)/settings/page.tsx`): win-back card + reviews card + monthly stats (win-backs this month + reviews this month, queried from `calls.winback_sent_at` and `review_requests.sent_at`). Save patches businesses columns directly (no `notifications_config` JSON). **Admin parity**: `/admin/clients/[clientId]/portal/inbox` and `/train` are thin server pages that reuse the same `InboxView` and `TrainView` client components — they pass `adminClientId={clientId}` which routes API calls via `?adminClientId=...`. The existing `/admin/clients/[clientId]/portal/settings` redirects through the impersonate flow so the new Automation tab is reachable via that path automatically. **Admin sidebar** (`src/components/admin/admin-portal-shell.tsx`) gains Inbox + Train TalkMate. **Client sidebar** gains both with realtime unread-count badge on Inbox; Starter sees a GROWTH locked tag on Inbox per pricing matrix. **`/lib/resolve-business.ts`** new helper centralises the dual-mode auth (owner_user_id vs `requireAdmin()` + `adminClientId`) used by all SMS + KB routes. **Website (talkmate-website)** — homepage gains "Customer care, automated" section between Integrations and Industries (4 cards), `/features` gains 4 new CORE entries, `PricingCards` adds Train/Win-back/Reviews to Starter and Two-way SMS Inbox to Growth. No em dashes; no Vapi/Twilio/Make.com mentions. **vercel.json**: 2 new cron entries (`kb-sync */5 * * * *` + `review-follow-up */15 * * * *`). **No new env vars required.** **Brief deviations applied + documented in DEPLOYMENT.md**: migration numbers renumbered 038/039/040 → 060/061/062; `businesses.vapi_agent_id` (not `vapi_assistant_id`); `businesses.plan` (not `plan_type`); `contacts.client_id` (not `business_id`) — new tables use `business_id` per brief; reused `calls.duration_seconds` (no new column); RLS via `private.get_current_client_id()`; codebase Grok default; inlined HMAC-SHA1 (no twilio npm dep). **Verified end-to-end on preview** (`rgifivtzmjvanzqwgadq`): 5 new tables, 11+3+6 columns, 5 RLS policies, realtime publication for sms_messages + sms_conversations; KB insert + mark_pending trigger fires; SMS conversation upsert; sms_messages CHECK constraints (direction/status/sent_by) accept; review_requests insert; zero error logs. **Prod migration verified at parity post-merge.** **Twilio Console webhooks repointed** for `+61468024020` (GM Towing) + `+61468003053` (Spectrum Towing) → `https://app.talkmate.com.au/api/webhooks/twilio/sms-inbound` via Twilio REST API. Other 4 Twilio numbers untouched (demos, spare, Hayden dispatcher YES/NO loop). **Data fix in flight**: GM Towing's `talkmate_number` was stored as `+61 468 024 020` (with spaces) pre-Sprint 1; normalised to `+61468024020` so the inbound webhook's `.eq('twilio_phone_number', toRaw)` lookup matches Twilio's E.164 `To` field. **Known limitation (Session 59 follow-up)**: Vapi's outbound SMS auto-reply goes via Twilio REST directly with the same Twilio creds, NOT through `src/lib/sms.ts`, so Vapi's auto-replies don't currently log to `sms_messages`. Inbox shows customer inbound + human/winback/review outbound but NOT Vapi auto-reply. Twilio Messaging Service `statusCallback` URL can close that gap in a follow-on. **Manual handoff still owed**: mobile app (talkmate-mobile) inbox/train/settings surfaces — API contract is fixed by `src/app/api/sms/conversations/*` + `/api/knowledge-base/*` (see existing Phase 2 sales table for the pattern). PRs: #85 (Sprint 1 → dev), #86 (Vapi fan-out → dev), #87 (dev → main). **Sprint 1 follow-ups (PR #89, merged `ffdffb9`)**: admin `/admin/clients` gains 4 chips (Unread SMS / KB status / Win-back / Reviews) sourced from new columns + an aggregate on `sms_conversations.unread_count`. New `/api/webhooks/twilio/sms-status` route closes the "Vapi auto-reply not in Inbox" gap — captures the `sent` MessageStatus, dedups against rows already logged via `lib/sms.ts` (idempotency via `sms_messages.twilio_message_sid`), fetches Body from the Twilio REST API (status callbacks don't carry Body), upserts the conversation, inserts `sms_messages` with `direction=outbound, sent_by=vapi`. Twilio Console `StatusCallback` URL set on `+61468024020` + `+61468003053` via REST API (same trip as the inbound webhook switch). **Mobile scaffolding** shipped on `talkmate-mobile` `feature/sprint-1-inbox-train` (PR #2): `src/utils/talkmateApi.js` wraps the portal API with Supabase Bearer-token auth; `src/screens/client/InboxScreen.js` is a full list → thread → reply + AI-suggest screen; `src/screens/client/TrainScreen.js` is a read-only KB browser with Sync Now. Intentionally NOT wired into AppNavigator because the client tab bar has been dormant since Phase 2 sub-project 1 — these slot in when client mobile is resurrected, see `docs/sprint-1-inbox-train.md` in the mobile repo. **Make.com audit done** via API: scenarios 5684595 (Callback Reminder) is Telegram-only with no Twilio touch; 5668121 (Auto Agent Brief) was misnamed — it doesn't update any Vapi agent, just texts Irfan a heads-up SMS on new signup, so the BUSINESS KNOWLEDGE collision concern was a false alarm; 5684671 (Dispatch Job Notification) sends two Twilio SMS + a Telegram. PATCHed the `From` number on scenarios 5668121 and 5684671 from `+61468024020` (GM Towing) → `+61468005893` (Spare) so customer/driver replies don't land in GM Towing's Inbox or trigger Vapi auto-reply impersonating GM Towing. Both scenarios still carry Twilio Basic auth in plaintext HTTP headers — when `TWILIO_AUTH_TOKEN` is rotated, the base64 header in each must be updated too. **Make.com API access**: `MAKE_API_TOKEN` in Vercel env (production), region `eu1`. Future audits should read from env not chat. Full audit findings in `docs/sprint-1-make-com-handoff.md`. |
| 56 | 2026-05-29 | feature/session-56-demo-system | (pending) | 059 | Demo system. New route group `src/app/sales-demo/[industry]/*` — token-gated (DEMO_PORTAL_SECRET) interactive replica of the client portal pre-loaded with seeded demo data. Six tabs (Dashboard, Calls, Bookings, Services, Settings, Team). Settings POSTs to `/api/sales-demo/[industry]/update-settings` guarded by `.eq('is_demo', true)`. Team invite is UI-only (no DB write). `/sales/demo` rewritten — three-section hub replacing 14-card grid: Your Demo Agent (industry + phone + Load Agent button), Live Portal Preview (Open Demo Portal in new tab), Overview Video (YouTube/Vimeo/MP4 auto-detect or placeholder). Book Demo flow — new `POST /api/sales/leads/[id]/book-demo` sends Resend email with rep's Calendly link (fallback NEXT_PUBLIC_DEMO_CALENDLY_URL), advances lead to `demo_booked`, inserts `lead_activities` (`activity_type='demo'`) + `rep_notifications` (`type='demo_booked'`); new `BookDemoModal` wired into `lead-drawer.tsx` and `HitListClient.tsx` priority cards. Profile gains "Demo Settings" card with Calendly URL field (validated `https://calendly.com/` prefix client + server). Admin gains: `/admin/demo-accounts` page listing `is_demo=true` businesses with call/booking counts + Open Demo Portal + Impersonate buttons, "Demo Configuration" card in contractor detail view (industry dropdown + read-only Calendly link), new `POST /api/admin/sales-reps/[id]/demo-config` (writes `admin_audit_log` action `rep_demo_industry_updated`), "Demo Accounts" entry in `AdminSidebarLayout`. `.eq('is_demo', false)` added to 7 admin business queries to hide the demo from all client lists (single-row lookups correctly skipped). New `/api/cron/reset-demo-business` runs every 4h at minute 17 to restore mutable demo business fields from `DEMO_BUSINESS_SNAPSHOTS` in `src/lib/demo-config.ts`. Migration 059 (additive + idempotent): adds `sales_reps.demo_industry/demo_calendly_url`, `businesses.is_demo`, partial index on `is_demo=true`, extends `rep_notifications.type` CHECK with `demo_booked`. Repurposes existing business `ad380eb3-a0b5-4566-9107-e0b075ac48e8` (already owned by hello@talkmate.com.au — `owner_user_id` is UNIQUE so INSERT-new was not possible) as the towing demo: sets name "Gold Coast Towing (Demo)", phone/address/greeting, services jsonb, `is_demo=true`. Seeds 10 calls + 3 bookings with valid hex UUIDs (`de0a0c01-...` and `de0a0b01-...`; the brief's original `demo-towing-...` UUIDs were invalid hex and would have failed `::uuid` cast). Schema corrections vs the brief: `businesses.name` (not `business_name`); no suburb/state/postcode columns; `bookings` uses `client_id` FK and `caller_phone` (not `business_id`/`caller_number`); `lead_activities.activity_type` + required `title`; `rep_notifications.message` (not `body`); `business_services` table does not exist (services live in `businesses.services` jsonb). New env vars (set in Vercel before deploy): `DEMO_PORTAL_SECRET` + `NEXT_PUBLIC_DEMO_PORTAL_TOKEN` (must match, generate via `openssl rand -base64 32`), `NEXT_PUBLIC_DEMO_CALENDLY_URL` (= `https://calendly.com/hello-talkmate/30min`), `NEXT_PUBLIC_DEMO_VIDEO_URL` (empty). Mobile parity (`talkmate-mobile` DemoScreen / LeadDetailScreen / ProfileScreen rewrites) deferred to a separate session — full spec in DEPLOYMENT.md. |

---

## Agent Pipeline Infrastructure (installed 2026-05-25)

| File | Location | Purpose |
|------|----------|---------|
| CLAUDE.md | `C:\Users\info\talkmate-portal\CLAUDE.md` | Project brain — read by every Claude Code session |
| MEMORY.md | `C:\Users\info\talkmate-portal\MEMORY.md` | Session history — agents append after every session |
| builder.md | `.claude/agents/builder.md` | Writes and edits code. Full tools. Sonnet model. |
| validator.md | `.claude/agents/validator.md` | Read-only code QA. Checks TalkMate rules. No write access. |
| qa-tester.md | `.claude/agents/qa-tester.md` | Live browser testing via Playwright MCP. No write access. |
| reviewer.md | `.claude/agents/reviewer.md` | GREEN/YELLOW/RED deployment gate. Opus model. No write access. |
| build/SKILL.md | `.claude/skills/build/SKILL.md` | Full pipeline skill — builder → validator → QA → reviewer → report |
| Global CLAUDE.md | `C:\Users\info\.claude\CLAUDE.md` | Default working directory + pipeline hardcoded for all sessions |

### MCP Servers (Claude Desktop)
| Server | Status | Purpose |
|--------|--------|---------|
| playwright | running | Live browser QA testing |
| github | running | Direct repo read/write — system map updates, file access |
| supabase | running | Direct prod schema/policy changes — `apply_migration`, `execute_sql`, `get_advisors`, `list_tables` (Session 52 onwards) |

### Build Pipeline Flow
Every task runs automatically: Builder → Validator → QA Tester (Playwright) → Reviewer → Report to Irfan → Donna deploy prompt on approval.

**Working-model update 2026-05-25 (Session 38):** Irfan asked Claude to own the full pipeline end-to-end and stop delegating to Donna for routine sessions. Claude now executes: plan → build → validate → push → preview QA → merge to dev → merge to main → wait for prod deploy READY → Playwright smoke tests against prod → SYSTEM_MAP update → MEMORY append → report. Donna is still on the bench for tasks Claude flags as needing human-in-the-loop. See `~/.claude/projects/.../memory/working-model-no-donna.md`.

**Working-model update 2026-05-28 (Session 52):** Supabase MCP now in active use for autonomous DDL changes against the prod project (`mdsfdaefsxwrakgkyflr`). When a session needs a migration, Claude can `apply_migration` directly via the MCP rather than handing off to `supabase db push` or Donna. Files still get saved to `supabase/migrations/` for source-of-truth parity. The auto-mode classifier blocks **self-modification** of its own rules (e.g. adding `autoMode.allow` overrides to `~/.claude/settings.json` from inside a session) — that's correct safety behaviour and must be done manually by Irfan if he wants the per-action gate suppressed.

---

## Migration Registry

| # | File | What it does |
|---|------|--------------| 
| 001 | 001_initial.sql | Initial schema — all 10 core tables |
| 002 | 002_add_missing_columns.sql | Missing column additions |
| 003 | 003_add_transcripts.sql | Call transcripts |
| 004 | 004_contacts_table.sql | Contacts table |
| 005 | 005_partner_program.sql | Partner program |
| 006 | 006_add_stripe_customer_id.sql | Stripe customer ID on businesses |
| 007 | 007_master_brief.sql | Master brief schema |
| 008 | 008_crm_foundation.sql | CRM foundation |
| 009 | 009_crm_session2_indexes.sql | CRM indexes |
| 010 | 010_session3.sql | Session 3 changes |
| 011 | 011_admin_client_management.sql | Admin client management |
| 020 | 020_services_and_trade_type.sql | Industry service fields |
| 021 | 021_trial_mode.sql | Trial mode |
| 022 | 022_pending_payment_status.sql | Pending payment status |
| 023 | 023_receptionist_features.sql | Receptionist features |
| 024 | 024_dispatcher_system.sql | Dispatcher system |
| 025 | 025_businesses_owner_unique.sql | Duplicate-owner guard |
| 026 | 026_rbac_and_audit.sql | RBAC + audit log |
| 027 | 027_talkmate_command.sql | TalkMate Command |
| 028 | 028_vapi_call_id.sql | Vapi call ID |
| 029 | 029_agent_last_synced_at.sql | Agent last synced timestamp |
| 030 | 030_distance_quoting_and_scheduler.sql | Distance quoting + scheduler |
| 031 | 031_accounts_vip_scheduler.sql | Accounts, VIP bypass, native scheduler |
| 032 | 032_call_intelligence.sql | Call Intelligence tables + sms_type constraint |
| 033 | 033_sms_visibility.sql | SMS visibility + admin_sms_failures view |
| 034 | 034_golive_checklist.sql | Admin Go-Live Verification Checklist |
| 035 | 035_sms_counter_fix.sql | sms_used_this_month counter fix |
| 036 | 036_sales_hq.sql | Sales HQ — leads, commissions, sales_reps |
| 037 | 037_pricing_overhaul.sql | billing_cycle, setup_fee_waived, setup_fee_amount, won_billing_cycle, bonus_amount |
| 038 | 038_contractor_agreement_flow.sql | contractors, contractor_agreements, sales_scripts, script_acknowledgements, contractor_commissions |
| 039 | 039_agent_health_monitoring.sql | Agent health monitoring tables |
| 040 | 040_unified_rep_lifecycle.sql | contractors.sales_rep_id, portal_invited_at, portal_access_email; sales_reps.contractor_id, onboarded_via, is_legacy |
| 041 | 041_contractor_signature_metadata.sql | contractor_agreements.signature_method, signature_timestamp, ip_address |
| 042 | 042_session27_fixes.sql | businesses.signup_at, welcome_email_sent; commissions.clawback_period_ends_at; sms_log CHECK extended; admin_sms_failures view widened |
| 043 | 043_session28_fixes.sql | calls.intelligence_retry_count; partial index on (intelligence_status, created_at) WHERE status IN ('pending','error') |
| 044 | 044_sms_confirmation_loop.sql | bookings 'declined' status; sms_log adds dispatcher_job_notification/booking_received/booking_confirmed/booking_declined/dispatcher_reminder; bookings.confirmation_ref/dispatcher_notified_at/reminder_sent_at/confirmed_by_phone; unique index on confirmation_ref; partial index for pending dispatcher sweep |
| 045 | 045_session30_fixes.sql | sms_log CHECK constraint extended with `owner_booking_notification` (22 total types) |
| 046 | 046_drop_legacy_bookings_columns.sql | Drop 6 legacy bookings columns (`confirmation_sms_sent`, `booking_type`, `service_requested`, `preferred_date`, `preferred_time`, `notes`) with idempotent backfill and 4 pre-migration safety checks documented in comments for Donna |
| 048 | 048_dispatcher_driver_app.sql | Dispatcher + driver app schema — Sessions 36-37: driver tables, dispatch job lifecycle, vehicle/photo/signature support |
| 049 | 049_sales_hq_tools.sql | Session 41 — Three new tables: `lead_followups` (type: email/call_reminder; status: pending/sent/dismissed; dismissed_at), `rep_notifications` (type: proposal_opened/followup_due/deal_reassigned/commission_updated/new_lead_assigned; published to `supabase_realtime` for the NotificationBell), `proposal_tracking` (resend_email_id, opened_count, plan). New RPC `increment_proposal_opens(text)` — SECURITY DEFINER with pinned search_path, EXECUTE revoked from anon/authenticated, granted to service_role only; uses IF FOUND guard so unknown email_ids return zero rows. `sales_reps.notification_email` (rep reply-to). `businesses` adds: `payment_confirmed_at`, `onboarding_started_at`, `onboarding_completed_by`, `sales_rep_id` (FK sales_reps), `temp_password`, `welcome_email_sent`. `client_comms_log`: business_id now nullable, new `lead_id` (FK leads) + `onboarding_stage`, CHECK constraint `business_id IS NOT NULL OR lead_id IS NOT NULL`. 8 new indexes (lead_followups status/send_at/active reminders; rep_notifications read; proposal_tracking lead/resend; businesses pending onboarding; leads won unconverted). Wrapped in BEGIN/COMMIT; all CREATE POLICY preceded by DROP POLICY IF EXISTS for safe re-runs. |
| 047 | 047_dedupe_health_alerts.sql | One-time cleanup of duplicate unresolved `agent_health_alerts` rows (`alert_type = 'config_issue'` only) that accumulated before the Session 35 auto-resolve fix. For each (business_id, issue_code) pair with multiple unresolved rows, keeps the most recent open row and marks the rest `resolved_at = now(), resolved_by = 'session_35_dedup_cleanup'`. 1-hour buffer prevents racing live cron. Idempotent — `RAISE NOTICE` skip-log on a clean table. |
| 050 | 050_session42_lifecycle_integrity.sql | Session 42 additive — adds `businesses.vapi_phone_number_id`, `vapi_phone_unassigned_at`, `vapi_phone_unassigned_reason` (with CHECK on reason enum); new `stripe_webhook_events` table (event_id PK, admin RLS) for Stripe webhook idempotency; one-time UPDATE of plan drift (`'professional'` → `'pro'` on businesses + subscriptions; `'premium'` → `'starter'` on businesses); one-time UPDATE backfilling `vapi_phone_number_id` for GM Towing (`d28934a0-4e5a-4040-b62d-36c9761263ee`) and Spectrum Towing (`0e856abc-ee2d-4cec-87da-ea96a5fab6ca`). Intentionally additive — no CHECK constraint on `plan` here (deferred to 051) so old in-flight Stripe webhook handlers can't fail with CHECK violations during the deploy window. |
| 051 | 051_plan_check_constraint.sql | Session 42 — adds `CHECK plan IN ('starter','growth','pro')` to `businesses.plan` and `subscriptions.plan`. Applied AFTER migration 050 + after new code is live on production. The split from 050 prevents the failure mode where old code's `(nickname || 'starter').toLowerCase()` could briefly write `'professional'` and violate the CHECK during the deploy gap. |
| 052 | 052_session43_sales_tooling.sql | Session 43 additive — `leads` adds `stripe_payment_link`, `stripe_payment_link_created_at`, `payment_confirmed_at` + partial index for payment-link tracking. New `public.admin_settings (key text PK, value text NOT NULL, updated_at, updated_by uuid → auth.users)` cross-cutting key/value store with admin-only RLS (service-role bypass). Seeded three keys for the Sales Pipeline page: `sales_sprint_start` (current AEST month start), `sales_sprint_end` (current AEST month end), `sales_mrr_target=26000`. `lead_activities.activity_type` CHECK constraint extended to allow `'reassign'` (idempotent DROP + ADD). |
| 053 | 053_critical_rls_fixes.sql | Session 52 — closes the Supabase security email's critical "Table publicly accessible" finding. `ALTER TABLE … ENABLE ROW LEVEL SECURITY` on `webhook_debug`, `public_holidays`, `client_golive_checklist`. Adds `public_holidays_select_authenticated` (`FOR SELECT TO authenticated USING (true)`) so the cookie-auth `/api/portal/public-holidays` route keeps returning rows for logged-in portal users; service-role writes via `/api/cron/sync-public-holidays` are unaffected (service role bypasses RLS). Applied via the Supabase MCP `apply_migration`, not `supabase db push`. |
| 054 | 054_security_advisor_hardening.sql | Session 52 — clears the ERROR-level SECURITY DEFINER view + the 7 mutable-search-path WARNs + the 8 RLS-on-no-policy INFOs. (1) Recreates `public.admin_sms_failures` view `WITH (security_invoker = true)` so it inherits the caller's privileges instead of the owner's. (2) `SET search_path = public` on `get_current_client_id`, `enforce_single_active_script`, `update_sales_updated_at`, `scheduler_settings_touch_updated_at`, `update_updated_at_column`, `update_dispatch_updated_at`, `generate_dispatch_job_number`. (3) Adds a `"service_role_only"` RESTRICTIVE deny-all policy (`FOR ALL TO anon, authenticated USING (false) WITH CHECK (false)`) on 8 tables that already had RLS-on-no-policy: `admin_audit_log`, `admin_settings`, `contractor_agreements`, `contractor_commissions`, `contractors`, `sales_scripts`, `script_acknowledgements`, `stripe_webhook_events` — purely cosmetic (RLS-on-no-policy was already deny-all for non-service-role) but silences the linter and documents intent. (4) Adds `COMMENT ON FUNCTION` strings to `current_rep_id`, `get_current_client_id`, `is_super_admin` documenting that they're RLS helpers (their direct-RPC exposure is closed in migration 056 below). Applied via the Supabase MCP. **Heads-up**: collides with an in-progress parallel `054_scheduler_bizzow_grid.sql` filename in the working tree on `dev` (Bizzow scheduler workstream); the scheduler migration is not yet applied and will need to be renumbered to `057_scheduler_bizzow_grid.sql` or later before it merges. |
| 055 | 055_silence_remaining_rls_info.sql | Session 52 — same `"service_role_only"` RESTRICTIVE deny-all policy applied to the two tables enabled by migration 053 (`webhook_debug`, `client_golive_checklist`). Was a follow-up because the advisor re-ran after migration 054 still listed them as RLS-on-no-policy INFOs. Applied via the Supabase MCP. |
| 056 | 056_move_rls_helpers_to_private_schema.sql | Session 52 — moves the three SECURITY DEFINER RLS helper functions (`current_rep_id`, `get_current_client_id`, `is_super_admin`) out of `public` (where PostgREST auto-publishes them at `/rest/v1/rpc/<name>` to anyone with the anon key) and into a new `private` schema (which PostgREST does not expose). `CREATE SCHEMA IF NOT EXISTS private`; `GRANT USAGE ON SCHEMA private TO anon, authenticated, service_role`; `ALTER FUNCTION public.<fn>() SET SCHEMA private` for each of the three. **Critical safety detail**: Postgres tracks RLS-policy → function dependencies by OID, not by textual name, so all 64 RLS policies on `public.*` tables + `storage.objects` continue to resolve the helpers without any policy rewrites; Postgres automatically regenerates the displayed policy expression text to use the new schema-qualified names (e.g. `(client_id = private.get_current_client_id())`). EXECUTE grants follow the function across the schema move, so anon/authenticated still satisfy the EXECUTE check during RLS evaluation. Verified post-migration: all three functions live in `private`, sample 5 policies on `public.bookings` / `callbacks` / `command_bots` etc. show the rewritten `private.<fn>()` text, advisor no longer flags any SECURITY DEFINER WARNs. Applied via the Supabase MCP. |
| 058 | 058_lead_followup_at.sql | Add `leads.next_followup_at timestamptz` + partial index — per-lead reminder timestamp for mobile Followup picker (Phase 2 sub-project 1) |

---

## Cron Jobs

| Path | Schedule (UTC) | AEST equiv | Purpose |
|------|---------------|------------|---------|
| /api/cron/vapi-health | `* * * * *` | Every minute | Vapi health ping |
| /api/cron/health-monitor | `*/5 * * * *` | Every 5 min | System health alerts |
| /api/cron/score-pending-calls | `*/10 * * * *` | Every 10 min | AI call scoring |
| /api/cron/stripe-sync | `*/15 * * * *` | Every 15 min | Stripe subscription sync |
| /api/cron/waitlist-expiry | `*/15 * * * *` | Every 15 min | Expire waitlist slots |
| /api/cron/usage-monitor | `*/30 * * * *` | Every 30 min | Usage monitoring |
| /api/cron/agent-health-check | `*/30 * * * *` | Every 30 min | Vapi agent health |
| /api/cron/abandoned-signup | `0 * * * *` | Every hour :00 | Abandoned signup recovery |
| /api/cron/email-triggers | `15 * * * *` | Every hour :15 | Lifecycle email triggers |
| /api/cron/sms-reminders | `30 * * * *` | Every hour :30 | SMS reminders (24h, 2h, + Session 29 dispatcher 15-min reminder sweep) |
| /api/cron/nps-check | `0 1 * * *` | 11:00 AEST | NPS check |
| /api/cron/sync-public-holidays | `0 0 1 1 *` | Jan 1 annually | Sync public holidays |
| /api/cron/data-retention | `0 0 1 * *` | 1st of month | Data retention cleanup |
| /api/cron/monthly-payouts | `0 23 1 * *` | 1st of month 09:00 AEST | Monthly payouts |
| /api/cron/daily-tasks | `0 14 * * *` | 00:00 AEST | Daily task sweep |
| /api/cron/db-backup | `0 16 * * *` | 02:00 AEST | DB backup |
| /api/cron/onboard-day7 | `0 22 * * *` | 08:00 AEST | Day-7 onboarding email |
| /api/cron/expire-trials | `15 22 * * *` | 08:15 AEST | Expire overdue trials |
| /api/cron/daily-quality-digest | `30 22 * * *` | 08:30 AEST | Daily call quality digest to admin |
| /api/cron/expire-pending-payments | `45 22 * * *` | 08:45 AEST | Flip 24h+ pending_payment → trial |
| /api/cron/call-forward-check | `0 23 * * *` | 09:00 AEST | Call forwarding check |
| /api/cron/trial-reminders | `15 23 * * *` | 09:15 AEST | Trial reminder emails |
| /api/cron/clear-eligible-commissions | `30 23 * * *` | 09:30 AEST | Clear commissions past clawback |
| /api/cron/process-followups | `50 * * * *` | Every hour :50 | Process scheduled lead follow-up emails (Session 41) |
| /api/cron/dispatch-timeout | `*/5 * * * *` | Every 5 min | Dispatch job timeout handler (Sessions 36-37) |

---

## Key Routes

### Public (no auth)
| Route | Purpose |
|-------|---------|
| `/` | Landing / marketing |
| `/wl-preview/[slug]` | White-label preview for prospects (public, no redirect) |
| `/contractor-onboarding/[token]` | Contractor agreement signing flow |

### Portal (auth required → 307 to /login)
| Route | Purpose |
|-------|---------|
| `/onboarding` | Self-serve onboarding wizard (10 steps, Stripe step 9) |
| `/dashboard` | Client dashboard |
| `/settings` | Business settings + notifications |
| `/billing` | Billing + plan comparison |

### Admin (admin email required → 307)
| Route | Purpose |
|-------|---------|
| `/admin/onboarding-queue` | Closed deals awaiting setup — promote leads, finish go-live, approve commissions (Session 41) |
| `/admin/onboarding-queue/[id]` | 5-step admin wizard — Step 1 promotes lead → business + auth user via `create-from-lead`; Step 5 calls `/api/admin/go-live` (Session 41) |
| `/admin/clients` | Client list + management — now includes "Closed by rep" column (Session 41) |
| `/admin/contractors` | Contractor list + detail + commissions |
| `/admin/sales-team` | Legacy rep management |
| `/admin/sales-scripts` | Script version control |
| `/admin/trials` | Trial management |
| `/admin/audit-log` | Audit log |
| `/admin/sms-failures` | SMS failure viewer |

### Sales (sales rep auth → 307)
| Route | Purpose |
|-------|---------|
| `/sales/dashboard` | Rep dashboard |
| `/sales/commissions` | Commission ledger with clawback dates |
| `/sales/contract` | Agreement on file (read-only) |
| `/sales/profile` | Rep profile — reply-to email for proposals (Session 41); in-session password change card (Session 50) |
| `/sales/demo` | Demo launcher — 14 industry cards, real-time Vapi agent swap on demo number (Session 41) |
| `/sales/hitlist` | Hit list — curated prospect targets (Session 41) |
| `/sales/leads/[id]/proposal` | Proposal builder + send, open tracking via Resend webhook (Session 41) |

---

## Key API Routes

| Method | Path | Auth | Purpose |
|--------|------|------|---------|
| POST | /api/auth/signup | public | Self-serve signup — writes signup_at, sends welcome email |
| POST | /api/stripe/embedded-checkout | session | Create embedded checkout session |
| GET | /api/onboarding/payment-status | session | Poll payment status after Stripe redirect |
| POST | /api/stripe/portal | session | Open Stripe billing portal |
| POST | /api/webhooks/stripe | Stripe sig | Stripe webhook handler |
| POST | /api/webhooks/vapi | Vapi sig | Vapi call webhook |
| POST | /api/twilio/sms-reply | Twilio sig (HMAC-SHA1) | Session 29 — dispatcher YES/NO reply for booking confirmation loop |
| GET | /api/cron/* | CRON_SECRET | All cron endpoints (401 without secret) |
| GET | /api/admin/* | admin | Admin-only API routes |
| POST | /api/sales/leads | sales rep | Sales rep creates a lead |
| POST | /api/sales/leads/[id]/won | sales rep | Mark lead won + record commission. Session 41: now also inserts a `rep_notifications` row + fires structured `sendInternalEmail` + writes `admin_audit_log` action `deal_closed_by_rep` (does NOT create businesses — that happens in the admin wizard). |
| POST | /api/sales/send-proposal | sales rep | Send proposal email from `sales@talkmate.com.au` (replyTo = rep `notification_email`); INSERT proposal_tracking with Resend id; flips lead to `proposal_sent` (Session 41) |
| POST | /api/sales/launch-demo | sales rep | Swap demo phone `assistantId` to one of 14 industry templates; refuses PATCH if current assistant not in `ALLOWED_CURRENT_ASSISTANTS` (Session 41) |
| POST | /api/sales/leads/[id]/followups | sales rep | Insert N pending `lead_followups` rows for the proposal sequence (Session 41) |
| PATCH | /api/sales/followups/[id]/dismiss | sales rep | Dismiss a Priority 1 call reminder on the Hit List (Session 41) |
| GET | /api/sales/platform-stats | sales rep | Aggregate active client count + MRR for the Live Clients dashboard card; revalidate 300s (Session 41) |
| PATCH | /api/sales/profile | sales rep | Save phone + new `notification_email` (validated for `@`) (Session 41 enhancement) |
| GET | /api/sales/me | sales rep (cookie or Bearer) | Mobile rep identity (Phase 2 sub-project 1) |
| GET | /api/sales/leads | sales rep (cookie or Bearer) | Mobile lead list, filter by ?status / ?include=bad_lead (Phase 2 sub-project 1) |
| GET | /api/sales/activity?since=today\|week\|month | sales rep (cookie or Bearer) | Mobile activity feed (Phase 2 sub-project 1) |
| GET | /api/sales/commissions | sales rep (cookie or Bearer) | Mobile commission ledger; response reshapes `commission_amount` → `base_amount` and computes `total = base + bonus` (Phase 2 sub-project 1) |
| GET | /api/sales/pipeline | sales rep (cookie or Bearer) | Mobile kanban with 7 in-flight stage counts + grouped leads (Phase 2 sub-project 1) |
| PATCH | /api/sales/leads/[id] | sales rep (cookie or Bearer) | Update lead fields including `next_followup_at` (Phase 2 sub-project 1) |
| POST | /api/admin/onboarding-queue/create-from-lead | admin | Promote won lead → businesses row + Supabase auth user. Pre-flights `auth.admin.listUsers()` for email collision (409 with `existing_user_id`); rolls back orphan auth user on business INSERT failure; uses `crypto.randomBytes(12).toString('base64url').slice(0,12)` for temp_password (NOT `Math.random`) (Session 41) |
| POST | /api/admin/go-live | admin | Calls `provisionAgent()` (Twilio + Vapi + checklist gate), flips `account_status='active'`, sends welcome email (gated on `welcome_email_sent=false`), approves all pending commissions for the business + inserts `rep_notifications` per affected rep (Session 41) |
| POST | /api/admin/businesses/[id]/resend-welcome | admin | Recovery route — regenerates temp_password, updates Supabase auth password BEFORE re-sending email (critical ordering: stale password in inbox would lock the client out); refuses if `welcome_email_sent=true` (Session 41) |
| GET\|POST | /api/admin/comms-log | admin | Read + write `client_comms_log` notes against either `business_id` or `lead_id`; powers ClientCommsLog component on queue cards + wizard (Session 41) |
| POST | /api/admin/approve-agent | admin | Session 41: rewritten as thin wrapper over `provisionAgent()` — external request/response contract preserved; "You're live" welcome email + Telegram alert stay in the wrapper, not the lib |
| POST | /api/contractors/invite | admin | Invite contractor |
| POST | /api/contractor-onboarding/[token]/sign | public | Contractor signs agreement + generates PDF |
| POST | /api/contractors/[id]/resend | admin | Resend contractor invite |
| POST | /api/contractors/[id]/terminate | admin | Terminate contractor |
| POST | /api/webhooks/resend | Resend sig (whsec_) | `email.opened` events for proposal open tracking (Session 41) |
| POST | /api/admin/commissions/[id] | admin | Approve commission (blocks if < 14 days) |

---

## Environment Variables

| Var | Required | Purpose |
|-----|----------|---------|
| NEXT_PUBLIC_SUPABASE_URL | ✅ | Supabase project URL |
| NEXT_PUBLIC_SUPABASE_ANON_KEY | ✅ | Supabase anon key (subject to RLS) |
| SUPABASE_SERVICE_ROLE_KEY | ✅ | Supabase service role (bypasses RLS) |
| STRIPE_SECRET_KEY | ✅ | Stripe API key |
| STRIPE_WEBHOOK_SECRET | ✅ | Stripe webhook signature verification |
| STRIPE_PRICE_STARTER | ✅ | Stripe price ID — Starter $299/mo |
| STRIPE_PRICE_GROWTH | ✅ | Stripe price ID — Growth $499/mo |
| STRIPE_PRICE_PROFESSIONAL | ✅ | Stripe price ID — Pro $799/mo |
| NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY | ✅ | Stripe publishable key for EmbeddedCheckout |
| RESEND_API_KEY | ✅ | Resend transactional email |
| RESEND_WEBHOOK_SECRET | ✅ | Resend webhook sig verification — `email.opened` proposal tracking (Session 41) |
| SALES_EMAIL_FROM | ✅ | From address for proposal emails — `sales@talkmate.com.au` (Session 41) |
| VAPI_DEMO_PHONE_NUMBER_ID | ✅ | Vapi phone number ID for demo line (`d1cfa481` = +61 752 409 791) — `/sales/demo` swaps active agent (Session 41) |
| NEXT_PUBLIC_DEMO_PHONE_DISPLAY | ✅ | Human-readable demo number shown on `/sales/demo` (Session 41) |
| TELEGRAM_BOT_TOKEN | ✅ | Telegram admin alerts |
| TELEGRAM_ADMIN_CHAT_ID | ✅ | Telegram chat ID for admin alerts |
| CRON_SECRET | ✅ | Bearer secret for all /api/cron/* routes |
| NEXT_PUBLIC_APP_URL | ✅ | https://app.talkmate.com.au |
| VAPI_API_KEY | ✅ | Vapi agent management |
| VAPI_WEBHOOK_SECRET | ✅ | Vapi webhook verification — MANDATORY from Session 28; /api/vapi/functions returns 500 if unset |
| TWILIO_CONFIRMATION_NUMBER | ✅ | Session 29 — dedicated Twilio number for dispatcher confirmation SMS (+61 480 847 945). Inbound webhook routes to /api/twilio/sms-reply |
| SCORING_PROVIDER | optional | Call Intelligence provider switch (Session 36 + 37 hotfixes). Values: `anthropic` (default — Claude Sonnet `claude-sonnet-4-6`), `haiku` (Claude Haiku `claude-haiku-4-5-20251001` — ~12× cheaper than Sonnet, recommended for current scale), or `xai` (Grok `grok-4.20-0309-non-reasoning` via existing `GROK_API_KEY`). Any other value falls back to `anthropic`. Set per-environment so production can run on Haiku/Grok while preview/dev stays on Anthropic. |
| ADMIN_EMAIL | recommended | Personal super-admin email(s) alongside hello@talkmate.com.au. Comma-separated supported (Session 30). |
| INTERNAL_ALERT_EMAIL | optional | Fallback admin email for internal alerts. Comma-separated supported (Session 30). |
| ~~CONTRACTOR_AGREEMENT_WEBHOOK_URL~~ | dead | Session 44 cutover: no longer read by code (Resend replaced Make.com). Safe to remove from Vercel on next cleanup. |
| ~~CONTRACTOR_SIGNED_PDF_WEBHOOK_URL~~ | dead | Session 44 cutover: no longer read by code (Resend replaced Make.com). Safe to remove from Vercel on next cleanup. |

---

## Pricing Reference (server-side source of truth)

| Plan | Monthly | Annual (10 mo) | Setup fee | Monthly commission | Annual bonus (2.5%) |
|------|---------|----------------|-----------|-------------------|---------------------|
| Starter | $299/mo | $2,990/yr | $299 | $299 | $74.75 |
| Growth | $499/mo | $4,990/yr | $349 | $349 | $124.75 |
| Pro | $799/mo | $7,990/yr | $399 | $399 | $199.75 |

Commission amounts are hardcoded server-side in `src/lib/commission.ts` and `src/lib/pricing.ts`. Never trust client input for these.

---

## Known Gaps / Deferred Work

### High Priority (from Session 27 audit — not yet addressed)
- **H12–H15** — Vapi agent health alerts (note: Session 28 reused the H8–H15 labels for its own four parts; the original audit items here remain)
- **H29, H32** — Remaining deferred audit items

### Preview Supabase schema drift — RESOLVED in Session 57 (2026-05-29)
~~The preview Supabase project (`rgifivtzmjvanzqwgadq`) was significantly behind prod (`mdsfdaefsxwrakgkyflr`): 15 tables missing entirely, `businesses` missing 20 columns, `dispatch_jobs` missing 45 columns, etc.~~ Fixed in Session 57 via a three-phase rebuild applied through the Supabase MCP `apply_migration` tool on the preview project: (1) replay of migration files 001-058 in order via subagent (44 clean, 6 BENIGN errors, 1 BUG on migration 046 around the `driver_visible_bookings` view depending on `notes`), (2) one consolidated `preview_parity_patch_20260529` migration adding 5 missing businesses columns (agent_status, preview_number, agent_approved_at, agent_approved, is_demo) + 2 sales_reps columns (demo_industry, demo_calendly_url) + missing `webhook_debug` table + the `businesses_agent_status_check` CHECK + the `idx_businesses_is_demo` partial index + the `rep_notifications.type` CHECK extension for `demo_booked`, (3) one `preview_drop_legacy_bookings_cols` migration removing the 5 truly-legacy bookings columns that 046 couldn't drop (`confirmation_sms_sent`, `booking_type`, `service_requested`, `preferred_date`, `preferred_time`). The `notes` column is intentionally kept on both prod and preview because `driver_visible_bookings` view references it. Verification: prod and preview both at 77 public tables / 1092 public columns. Preview Vercel deploys can now E2E-verify any feature that uses post-Session-30 schema. **Going forward, new prod migrations should also be applied to preview via `apply_migration` on `rgifivtzmjvanzqwgadq` to keep parity.** See memory note `preview-supabase-schema-drift.md` for the procedure if drift recurs.

### Embed bug fixed in Session 56
`/admin/clients` previously used PostgREST embedded join `sales_reps:sales_rep_id(full_name)` which had been silently returning empty data since Session 41 — the CLOSED BY REP column always rendered "—" even for businesses with an assigned rep, because the FK metadata is missing from PostgREST's schema cache for this project. Fix shipped in commit `e33edb4`: drop the embed, fetch sales_reps by id list in a second query, map back. CLOSED BY REP will populate correctly once a real rep is assigned to a business.

### Security — Daily Reminder (added 2026-05-28, remove each line as completed)
**ROTATE 4 LEAKED TOKENS.**

From Defender threat history (false-positive `LummaStealerClick` detections of Claude Code's own PowerShell API calls between 14/05 and 25/05 — tokens permanently captured in the event log):
- [ ] Telegram bot token `8191514620:AAHP83o-jgZs_uTH2wxmksV3xR6Ob3WtPdk` → BotFather `/revoke` then `/token` to regenerate
- [ ] Telegram bot token `8537129320:AAFINSRO9_XCv-dZkIHTitIohsXGmLoEExc` → same procedure
- [ ] Make.com API token `52f27cfc-7b0d-4f72-ad22-e87f4889ad81` → Make.com profile → API tokens → regenerate

From CTS-Portal git remote (found 2026-05-28 while deleting local workspace; `https://github.com/irfanhanif89-art/CTS-Portal` had a GitHub PAT embedded in the `.git/config` origin URL — meaning anyone with read access to that folder's `.git` directory had push rights to the repo):
- [ ] GitHub PAT `github_pat_11CBP27WY0…` → GitHub Settings → Developer settings → Personal access tokens → Revoke. If you still need push access, generate a fresh fine-grained token scoped just to CTS-Portal and store via `git credential-manager`, not in the URL.

After rotation, update any TalkMate Vercel env vars / Make.com scenarios that reference the rotated tokens. When all 4 are checked, delete this whole reminder block.

### Closed in Session 42
- **H8** — Vapi agent now deprovisioned on cancel/expire/suspend via Lever 2 (`unassignVapiPhone` PATCHes `phoneNumber.assistantId = null`). Reversible on reactivation via `reassignVapiPhone`. The assistant body is never touched, so model/voice/tools tuning is preserved across pause-resume cycles. `src/lib/vapi-phone.ts` is the canonical helper.
- **H9** — Closed as "already enforced at DB level." Original brief premise was false: `bookings.client_id` and `sms_log.client_id` are both NOT NULL with FK to `businesses(id)` ON DELETE CASCADE. Postgres physically rejects orphan inserts and CASCADE prevents retroactive orphans on business deletion. Production check confirmed 0 orphans. Additional defensive Telegram alert added in `/api/vapi/functions` `createBooking` for codes 23502 (not_null_violation) and 23503 (foreign_key_violation) so any future schema drift surfaces immediately. The brief's flag column, admin Data Integrity tab, and INSERT guards are all redundant with the DB layer.
- **H10** — Stripe `customer.subscription.updated` now syncs `businesses.plan` via `planFromStripePriceNickname()` with compare-and-update (no Telegram noise on renewal events). All 3 unsafe `(nickname || 'starter').toLowerCase()` writes replaced. CHECK constraint `plan IN ('starter','growth','pro')` added at DB level via migration 051. `'professional'` and `'premium'` drift in production data cleaned up by migration 050. Read-side bandages cleanup: `'professional'` removed from the `Plan` union in `src/lib/plan.ts` and 22 dependent sites simplified (mostly `plan === 'pro' || plan === 'professional'` → `plan === 'pro'`). `getPlan()` kept defensive `'professional'` → `'pro'` normalisation for any stale read replicas.
- **Stripe webhook idempotency** (cross-cutting fix surfaced by council review) — new `stripe_webhook_events` dedup table protects every case in the switch from Stripe's guaranteed retries.

### Closed in Session 28
- Vapi function endpoint auth — `VAPI_WEBHOOK_SECRET` is now mandatory, legacy `business_id` trust replaced with assistantId lookup.
- Call intelligence resilience — outer catch stamps error status; error rows get 7-day retry window; dead CRITICAL_FLAG_TYPES entries removed.
- Agent config standard — restructured into required / requiredForBookings / requiredForQuoting; shared TOOL_DEFS module; plan-aware validator; onboarding builds validator-clean agents on first try.
- Approve-agent governance — switched to `requireAdmin()`; gated on the go-live checklist with `?override=true` escape hatch + Telegram alert.

### Closed in Session 30
- **H18** — Sync routes were writing the wrong `serverUrl` (`/api/webhooks/vapi` instead of `/api/vapi/functions`). Both sync routes fixed; `backfill-server-url` cron repairs the existing live assistants.
- **H21** — Admin PATCH whitelist expanded to all 7 valid `account_status` values + `billing_cycle`, `setup_fee_waived`, `setup_fee_amount`. Edit modal exposes the new billing fields.
- **H27** — SMS infrastructure failures (twilio_error, config_missing, invalid_phone) now fire a Telegram alert via `sendAdminTelegram`. Plan-rule rejections stay silent.
- **H28** — Welcome email moved from `onboarding/complete` (premature — agent wasn't live yet) to `admin/approve-agent` non-override path (fires only when go-live checklist is clean).
- **H30** — Owner booking notification SMS — new `owner_booking_notification` type + template + createBooking call site. Gated on `notifications_config.alert_owner=true` + `owner_number`.
- **H31** — Impersonation route gains `?redirect=1` + `?next=` modes. 7 admin stub pages now redirect through impersonation into the real client portal pages.
- **H33** — Calls page no longer hangs on "Loading…" for logged-out users (sets loading=false in the early-return).

### Closed in Session 31
- **H11** — Legacy Stripe checkout routes (`/api/stripe/checkout`, `/api/stripe/create-checkout-session`) deleted after grep confirmed zero in-repo callers. Embedded checkout + billing portal cover all current flows.
- **H19, H20** — Bookings page schema modernized to read `scheduled_start`, `truck_type`, `description`, `pickup_address`, `dropoff_address`, `confirmation_ref`, `sms_confirmation_sent`. Legacy fields kept as optional fallbacks; pickup → dropoff route line + REF badge + New Booking modal added.
- **Backfill cron** — `/api/cron/backfill-server-url` route + `vercel.json` entry removed (both live Vapi agents verified by Donna to have the correct `serverUrl`).

### Closed in Session 32
- **M34** — `LEAD_STATUS_COLUMNS` now includes `'nurture'`. Leads moved to Nurture appear on the kanban instead of vanishing.
- **M5** — Dashboard "Calls missed this week" label corrected to "Calls missed this month".
- **M8** — Missed-call filter fixed. Dashboard query now selects `intelligence_status`; counts only `outcome === 'Missed'` OR null-outcome calls where `intelligence_status IN ('resolved','review','critical')` AND `duration_seconds < 5`. In-progress calls (`pending`/`error`) are correctly excluded.
- **L5** — Annual ROI calculation uses `biz.billing_cycle`; annual subscribers see years × $2990/$4990/$7990 instead of months × monthly price.
- **M35** — `commissionPaidEmailHtml` template added to `sales-notify.ts`; `/api/admin/commissions/[id]` PATCH fires the email on pay. Rep + business pulled from existing JOIN — no separate `businesses` query.
- **M6** — Help link added to portal sidebar — `<a href="mailto:hello@talkmate.com.au?subject=TalkMate%20Portal%20Help">`, `HelpCircle` icon, rendered outside the section loop because mailto cannot use Next.js `<Link>`. Never highlights as active.
- **L1** — Hardcoded estimates centralised. `src/lib/dashboard-defaults.ts` exports `INDUSTRY_AVG_UPSELL_PER_CALL` (6.20) and `INDUSTRY_AVG_CALL_VALUE` (85). Five literal occurrences replaced across dashboard-client / dashboard page / billing page.

### Closed in Session 33
- **Legacy bookings columns dropped.** Migration 046 drops `confirmation_sms_sent`, `booking_type`, `service_requested`, `preferred_date`, `preferred_time`, and `notes` after backfilling `sms_confirmation_sent` and `description` idempotently. Five code files updated in the same commit so the migration is safe to apply. Pre-migration checks documented as SQL comments for Donna.
- **L7 — Stripe pagination.** `/api/cron/stripe-sync` now paginates with `starting_after`. Safety cap at 50 pages = 5,000 subscriptions per run.

### Deferred

### Infrastructure
- ~~**PDF template** (`/public/templates/contractor-agreement-template.pdf`) not yet uploaded — fallback inline PDF is used~~ — **CLOSED in Session 46**: the template was actually committed in Session 22 but contained literal `{{placeholder}}` Mustache text that pdf-lib could not substitute, so every signed contractor since 2026-05-20 received a PDF with raw placeholder text in the body. Template deleted; `generate-contractor-pdf.ts` rewritten to produce a full programmatic 7-page v2.0 agreement with real data filled in everywhere. **Forward-only** — pre-Session-46 signed copies (Jade, Navya, ProdTest Claude) still have polluted PDFs in `contractor-agreements` storage; re-sign would require a new invite per contractor.
- ~~**Make.com scenarios** not yet built (contractor invite email + signed PDF delivery)~~ — **CLOSED in Session 44**: cutover to Resend in `src/lib/contractor-webhooks.ts`. Env vars `CONTRACTOR_AGREEMENT_WEBHOOK_URL` and `CONTRACTOR_SIGNED_PDF_WEBHOOK_URL` are no longer read; safe to remove from Vercel on next cleanup.
- **Supabase Auth — leaked password protection disabled** (opened in Session 52). Supabase's `auth_leaked_password_protection` security advisor is currently WARN — Supabase Auth is not checking new passwords against HaveIBeenPwned.org. Cannot be enabled via SQL or the Supabase MCP currently in use; requires a manual toggle in **Supabase Dashboard → Authentication → Policies → Password Strength → "Check passwords against HaveIBeenPwned.org"**. Once enabled, every sign-up + password reset + admin-driven password change will be rejected if the chosen password is in any public breach dump. No code change required on our side — the check runs inside Supabase Auth at the moment of `auth.users` write.
- **Call-forwarding broken for three live clients** (surfaced in the Session 55 E2E audit). `system_alerts` shows the daily `/api/cron/call-forward-check` job has been firing unresolved `call_forward_broken` warnings every day from at least 2026-05-23 onwards for **GM Towing**, **Spectrum Towing**, and **Rapid Plumbing & Gas**. Message: *"Daily call-forward test failed for X. Check phone forwarding settings."* All three rows are `severity='warning', resolved=false`. Root cause not yet investigated — could be (a) the Telstra/Optus carrier conditional-forward setting lapsed on the owner's mobile, (b) the test routine itself has stale assumptions, or (c) Vapi changed the inbound-path semantics. The agents themselves are still answering calls (`/api/webhooks/vapi` traffic is healthy), so the immediate customer impact is the unanswered-overflow forward path, not first-ring pickup. **Next action**: investigate `/api/cron/call-forward-check` logic + dial each forwarding number manually before contacting the owners. Telegram pings to admin presumably already fired daily; check the chat history.

### Planned follow-on sessions
- Vapi lifecycle brief
- SMS/bookings data integrity brief
- Admin tooling completeness brief
- **Mobile Sales Rep — Phase 2 API integration.** Build the 7 `/api/sales/*` endpoints in the portal (GET `/me`, GET `/leads`, PATCH `/leads/[id]`, POST `/leads/[id]/notes`, GET `/activity`, GET `/commissions`, GET `/pipeline`). Mark-won already exists from Session 41. All gated on `sales_reps.status='active'` RLS. Swap `mockLeads` / `mockLeadActivities` / `mockCommissionsForRep` in the talkmate-mobile repo for `fetch()` calls of the same shape (shape contract is locked by `src/data/mockData.js`). Add `expo-secure-store` for token persistence. Defer mobile drag-and-drop kanban and in-app voice recording (`expo-audio` + portal `/api/voice-transcribe`) to Phase 3.

---

## Supabase Storage Buckets

| Bucket | Access | Purpose |
|--------|--------|---------|
| contractor-agreements | Private | Signed contractor agreement PDFs — 365-day signed URLs for delivery |

---

## Test Accounts

Dedicated mailbox for Claude-run QA. Use for any test that requires receiving a real email (contractor invites, welcome emails, proposal sends, etc.). Do NOT use for live customer-facing flows.

| Field | Value |
|-------|-------|
| Email | `testingtalkmate@gmail.com` |
| Gmail password | `Talkmatetest2026!` |
| Purpose | Receive test emails sent by Claude during QA |
| Created | 2026-05-26 |

After a QA test, delete any test rows created in `contractors` / `leads` / `businesses` / `sales_reps` / `contractor_agreements` / `script_acknowledgements` so they don't pollute reporting.

### Supabase password for testingtalkmate on prod

The `testingtalkmate@gmail.com` auth.users row exists on prod but has no Supabase password by default (the Gmail password is for receiving emails, not for signing in). To E2E-test a flow that needs the rep to actually log in (e.g. verify post-sign redirect chain), set a temporary Supabase password via service-role SQL, then reset it to NULL after the test. Recipe used in Session 46:

```sql
-- Set
UPDATE auth.users
SET encrypted_password = crypt('ProdTestRep2026!', gen_salt('bf')),
    email_confirmed_at = COALESCE(email_confirmed_at, now()),
    updated_at = now()
WHERE email = 'testingtalkmate@gmail.com';

-- Reset after test
UPDATE auth.users
SET encrypted_password = NULL, updated_at = now()
WHERE email = 'testingtalkmate@gmail.com';
```

Touching `auth.users` directly is a sensitive operation — only do this on the dedicated QA mailbox, never on a real customer or rep account.

---

## Client Surfaces

The web portal is the production system of record. There are two additional client surfaces that consume (or will consume) the portal's APIs:

### `talkmate-mobile` — Expo SDK 54 / React Navigation v7

Local repo: `C:\Users\info\.claude\WEBSITE BUILD\talkmate-mobile` (under git locally, initial commit `ebbede8`, branch `main`, no remote yet). Three user roles, gated in `src/context/AuthContext.js` and branched in `src/navigation/AppNavigator.js`:

| Role | Login email | Tabs |
|------|-------------|------|
| `client` (default) | anything not below | Home · Calls · Bookings/Dispatch/Contacts · Support |
| `admin` | `admin@talkmate.com.au` | AdminDashboard · Support |
| `sales_rep` | `rep@talkmate.com.au` | Leads · Pipeline · Activity · Commissions · Support |

**Sales Rep mode (Phase 1, shipped 2026-05-27)** — mock-data only. Five new screens under `src/screens/sales/`: `LeadsScreen` (filter chips, inline tap-to-call + SMS prefill), `LeadDetailScreen` (stage selector, Mark Won + Mark Lost + Followup modals, inline commission preview), `PipelineScreen` (6-column horizontal kanban, Lost hidden), `ActivityScreen` (Today/Yesterday/This week sections), `CommissionsScreen` (Pending/Approved/Paid tabs + policy modal). Mock data: 23 leads, 30 activities, 8 commission rows. Each mock shape is annotated with its target Supabase table for the Phase 2 swap.

**Phase 2 (planned)** — wire the mobile app to portal endpoints. The shape contract is fixed by `src/data/mockData.js` in the mobile repo:

| Mock data (mobile) | Phase 2 endpoint (portal) | Method |
|--------------------|---------------------------|--------|
| `mockSalesRep` | `/api/sales/me` | GET |
| `mockLeads` | `/api/sales/leads?assigned_to=me` | GET |
| (lead stage update) | `/api/sales/leads/[id]` `{ stage }` | PATCH |
| (mark won) | `/api/sales/leads/[id]/won` `{ plan, billing_cycle, setup_fee_waived }` | POST — **already exists from Session 41** |
| (add note) | `/api/sales/leads/[id]/notes` `{ body }` | POST |
| `mockLeadActivities` | `/api/sales/activity?rep=me&since=today` | GET |
| `mockCommissionsForRep` | `/api/sales/commissions?rep=me` | GET |
| (pipeline counts) | `/api/sales/pipeline?rep=me` | GET |

All endpoints will be gated on `sales_reps.status='active'` (mirrors the Session 41 `/api/sales/*` pattern). Mobile auth (Phase 2): Supabase magic link from the portal email path, persisted via `expo-secure-store`.

**Hard rules for mobile work:**
- `canUseCommand` in `AuthContext` returns `true` for sales reps regardless of business (reps have no business attached; the existing Starter-plan lock cannot gate them).
- Kanban cards use `+$X/mo` for forecast MRR — never `~$X/mo` (tilde reads as minus on a phone screen).
- `getMockPipelineCounts()` returns all 7 stages including `lost`. `PipelineScreen` filters Lost out of the kanban; LeadDetail's stage selector keeps it (reps must be able to mark Lost).
- Voice notes in Phase 1 are a plain `TextInput` — iOS keyboard mic handles dictation. Phase 2 swaps to `expo-audio` + a portal `/api/voice-transcribe` Whisper endpoint.
- Mobile reps never get a CSV uploader — bulk-import stays on `/admin/leads-import` in the portal.

**Network gotcha (Windows dev):** the dev machine's Wi-Fi profile is "Public", which blocks Expo's `--lan` mode from being reachable by the phone. Use `npx expo start --tunnel` instead (the `@expo/ngrok` dep is already installed in the mobile repo) until the profile is flipped to "Private" via `Set-NetConnectionProfile -NetworkCategory Private` (requires admin).

---

## Twilio Number Map

Live account SID + auth token live in Vercel env (`TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`). Not committed to this file because `talkmate-portal` is a public repo (verified via GitHub API `repo.private = false`) — GitHub push-protection actively blocks SID/token-shaped strings in committed files anyway, and the operational map below is enough to do every routine task without either secret being inline. Rotate the auth token in Twilio Console any time it has been pasted into a chat, an issue, or a screenshot; the Sprint 1 build (2026-05-31) is the most recent known exposure.

| Number         | Twilio SID                                    | Status         | Purpose                                | Inbound SMS webhook                                         |
|----------------|-----------------------------------------------|----------------|----------------------------------------|-------------------------------------------------------------|
| +61468024020   | `PNc2b7adbb974dafba4138838acf8982f1`          | Active         | **GM Towing** (live client)            | `https://app.talkmate.com.au/api/webhooks/twilio/sms-inbound` (Sprint 1) |
| +61468003053   | `PN39de0f96f3226e584c4d559a6888f512`          | Active         | **Spectrum Towing** (live client)      | `https://app.talkmate.com.au/api/webhooks/twilio/sms-inbound` (Sprint 1) |
| +61480847945   | `PN464e550fee32c0cd5bea4d5867dcbf5b`          | Active         | Hayden dispatcher YES/NO loop          | `https://app.talkmate.com.au/api/twilio/sms-reply` (Session 29)         |
| +61480892284   | `PNcbbb825136a5d9801c26efe644a94023`          | Demo           | Jade demo line — towing                | `https://api.vapi.ai/twilio/sms` (Vapi auto-reply)                      |
| +61468005893   | `PN64fed6cf25d8592002cabb7b6c573a14`          | Spare          | Next new client                        | `https://api.vapi.ai/twilio/sms` (Vapi auto-reply)                      |
| +61752409791   | `PNf14cfc0b4ae7832b31108bac6eb09b48`          | Future demo    | Landline demo agent                    | `https://api.vapi.ai/twilio/sms` (Vapi auto-reply)                      |

US toll-free `+18337624817` (SID `PNe937e46a7d6ed39b506700ba34a15728`) is a dev/test number routed to an ngrok URL; ignore for ops.

The Sprint 1 inbound webhook **fans out** to `https://api.vapi.ai/twilio/sms` after logging to `sms_messages` so Vapi auto-reply still fires for GM + Spectrum (otherwise the repointing would silence Vapi entirely). STOP/START keywords skip the fan-out for compliance.

`businesses.twilio_phone_number` + `twilio_phone_sid` populated for GM Towing and Spectrum Towing only. Other numbers (demos, spare, dispatcher loop) intentionally NOT mapped to a business row — they have no Inbox.

---

## Architecture Notes

- **Auth:** Supabase Auth (email/password). Sessions via SSR cookies.
- **Admin gate:** `requireAdmin()` checks email against the super-admin allowlist (hello@talkmate.com.au + comma-split `process.env.ADMIN_EMAIL` + comma-split `process.env.INTERNAL_ALERT_EMAIL`).
- **RLS bypass:** All server-side DB operations use `createAdminClient()` with service role key — `createClient` from `@supabase/supabase-js` direct, NOT `createServerClient` from `@supabase/ssr`.
- **RLS helpers live in `private` schema** (since Session 52). `private.current_rep_id()`, `private.get_current_client_id()`, and `private.is_super_admin()` are SECURITY DEFINER functions referenced by 64 RLS policies across `public.*` tables + `storage.objects`. They were moved out of `public` so PostgREST does not auto-expose them at `/rest/v1/rpc/<name>` to anon/authenticated clients. Postgres tracks the policy → function link by OID, so the schema move did not require any policy rewrites — but if you write a NEW policy or trigger that references one of these, write the call as `private.is_super_admin()` (schema-qualified) since the default search_path does not include `private`. Other in-database code (other functions, triggers) that needs the same helpers should grant itself USAGE on `private` and qualify the call site.
- **Stripe:** Live keys. EmbeddedCheckout for onboarding (step 9). Billing portal for plan changes. Webhooks verify signature.
- **Vapi:** Agents provisioned per client. Webhook receives call events, triggers scoring, SMS, booking creation. Session 41 extracts the provisioning core (Twilio AU mobile purchase + Vapi register + go-live checklist gate) into `src/lib/provisioning/approveAgent.ts`; both `/api/admin/approve-agent` (legacy) and `/api/admin/go-live` (new wizard) call it. Twilio's `Idempotency-Key` header is NOT honored on `IncomingPhoneNumbers POST` — application-level dedup via `businesses.phone_number IS NULL` is the only safe path. `phone_number` is persisted to DB BEFORE Vapi register so a crash mid-register doesn't double-buy on retry. Demo line `+61 752 409 791` (assistant `fdeef08c-...`) is repointed by `/api/sales/launch-demo`; safety allowlist `ALLOWED_CURRENT_ASSISTANTS` refuses to PATCH if the current assistant is unrecognised.
- **Resend webhook:** Signed via the Svix format (`svix-id`, `svix-timestamp`, `svix-signature` headers; base64 HMAC over `${id}.${timestamp}.${body}`). Verified with `Webhook(secret).verify(body, headers)` from the `svix` npm package — NOT a raw HMAC of the body. Endpoint `/api/webhooks/resend` listens to `email.opened` and calls the `increment_proposal_opens` RPC, which uses an `IF FOUND` guard so unrecognised email_ids return zero rows.
- **Commissions:** 14-day clawback enforced server-side. Admin cannot approve before `clawback_period_ends_at`. Backfilled by migration 042.
- **iOS/Safari auth:** Never use `supabase.auth.getSession()` client-side for critical flows. Stripe checkout uses plain `<a href>` links or EmbeddedCheckout.
