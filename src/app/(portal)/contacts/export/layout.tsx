import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Export Contacts' }

export default function ContactsExportLayout({ children }: { children: React.ReactNode }) {
  return children
}
