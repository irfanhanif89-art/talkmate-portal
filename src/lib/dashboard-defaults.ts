/**
 * Industry benchmark values used in ROI and revenue estimates.
 * These are Australian SMB averages used when per-client data is unavailable.
 * Source: TalkMate internal estimates based on client data.
 */
export const INDUSTRY_AVG_UPSELL_PER_CALL = 6.20   // avg additional revenue per answered call
export const INDUSTRY_AVG_CALL_VALUE = 85           // avg job/transaction value
